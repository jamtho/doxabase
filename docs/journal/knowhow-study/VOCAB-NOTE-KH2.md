# VOCAB-NOTE-KH2 — the know-how vocabulary, tranche 2 (understanding-lens revision), for James

**Date**: 2026-08-09. **Distiller tranche 2**, commissioned by your two reviews of that day: the understanding-lens critique ("the progressive build up of understanding BEFORE action is taken", obs 27da9e10) and the arc-analysis endorsement (decision = frame-adoption + accountability, "held weakly"; arcs as elements of graphs; obs 93587901).
**State**: ontology 250 → 442 triples, shapes 95 → 227, patterns 0 → 390 (the 13 arcs + all typed relations), map 315 → 370, observations +2 (the repaired arc onsets). Graph conforms scope "all"; zero staged debt; three applied revisions (5dace5e7 vocabulary, 02897ed6 arcs+relations, 62f55823 map instances) plus one deliberate negative probe, rejected by the new gate and closed discarded.
**Per your style law, worked examples first. Abstractions after.**

---

## 1. Worked example: the identity-freeze arc, end to end

This is the arc you promoted to the public root doc's centerpiece — here is exactly how it now lives in the capsule, from raw chunk to pattern node.

**The chunk.** Archive chunk-0018, source L2728, 2026-07-06T00:24 — session 1's landing briefing to you:

> "Static identity is frozen per year: 2,736 of 2,738 name transitions land exactly on Jan 1 — it's claiming your index builder canonicalizes identity annually…"

**The onset episode (NEW — this moment had no episode among all 249).** An anomaly is not a decision, so the decision-lens harvest filtered it out; the arc analysis proved the gap, and this revision repaired it. Observation `5b7eba52` (EPISODE-ONSET) now records the registration: the anomaly stated **with a falsifiable attribution hypothesis** (blame the index builder — wrong, and valuably so), a severity-graded caveat already recorded, and one of the three budgeted expert questions spent on it. That is what the beginning of understanding looks like in this corpus: no decision anywhere in sight.

**The member episodes, joined by typed relations.** The arc's spine is now queryable `kh:leadsTo` edges (graph-shaped, not a list — your "one leap powers many"):

```
5b7eba52 (onset: Jan-1 anomaly registered)
  → b145f424 (the decisive layer-diff experiment — inference rule stated first;
              index exonerated, freeze confirmed in raw broadcasts)
  → 26f11025 (provider docs REFINE the mystery — recorded as refinement-not-confirmation)
  → dde06ef7 (the pathology encoded INTO semantics: apparentDate vs datingQuality)
     [this node also sits in the AisIdentity arc — one leap powering two arcs]
  → fc198a80 (a cold agent honors datingQuality=year_only_undatable unprompted)
  → f14be00e (Jan-1 datingQuality becomes a SEALED grading trap)
  → 1f0f6451 (the concept becomes a design metaphor generating law in another territory)
  → 15f08d3f (you promote the whole arc to the public root doc's centerpiece)
```

**The pattern node.** `kha:identity-freeze` is a `kh:Arc` — and, deliberately, also a plain `rc:Pattern`: your Pattern machinery already said what an arc is ("a synthesis over observations… bridges point-in-time findings and the current project map"), so membership is ordinary `rc:supportingObservation`, and the workbench's pattern views get arcs for free. What arcs genuinely needed beyond Pattern, and only that, was minted:

- `kh:pivotQuote` (13/13 arcs, verbatim-verified): *"there's a decisive experiment hiding in your identity-freeze question... if broadcasts/ shows mid-year name changes while the index shows frozen identity, it's your index builder; if broadcasts are frozen too, it's the provider"*
- `kh:becameAutomatic` (13/13 — the completion criterion): *Jan-1 identity dates are never again read as real-world events; cold agents apply the caveat untaught and are graded on doing so; "silent identity maintenance destroys the audit trail" generates new law by analogy without fresh deliberation.*
- `kh:onsetMember` → 5b7eba52; `kh:onsetShape` → `kh:anomalyRegistered`.

The arc is complete precisely because its decisions stopped being decisions — which is your criterion, and it held for all thirteen.

## 2. Worked example: the elicitation loop, round 1 → round 9

The arc your critique most directly vindicates: it is entirely about understanding growing, and almost none of it is a decision.

**The chunk.** Archive chunk-0019, source L2731, 2026-07-06T18:15 — your first expert-channel reply:

> "I'm surprised static identity is frozen per year. The first question is whether this is a fact of the data source … or is a bug in my index file generation… I am confused by this."

**The onset episode (NEW).** Observation `4f51b4bf` records round 1 as the loop's question-quality baseline — and it already carries, unnamed, everything the loop later refined into protocol: you own your bugs cheerfully ("this will be my screw up, oops… Remind me to fix when we're not doing this!"), forward URLs as first-class evidence, seed the next direction ("These vessel stories are absolutely fantastic…"), and volunteer tradecraft nobody asked for — the shared-MMSI segregator, "use position to infer identity." A puzzled reply is not a decision, so no episode existed.

**The arc, with its typed relations.** `kha:elicitation-loop` runs: channel invented (c83fe21b) → your depth withheld as grading instrument (97bba402) → **round 1, the puzzled baseline (4f51b4bf)** → the 24-hour concede-record-commission-return loop (2a8b096a, which is `kh:commissionedBy` the naming-humility correction c86f5ee6 — the burst→session edge made queryable) → the 1am argument ending in your own restatement + the volunteered gear beacons (21ce14af) → the five-beat anatomy named (84889151) → **round 9: the system proposes your own round-1 segregator tradecraft back to you** (c78a2ac2) → the drip as a designed two-way surface (2bf750c5) → crediting done to your face (30f7612f).

- `kh:pivotQuote`: *"the human recognizes their own judgement coming back at them, applied correctly to a case they never discussed."*
- `kh:becameAutomatic`: *questions recorded before answers (holding at 01:19); replies partition concede/defend/synthesize; results return in your vocabulary; disclosure is provoked rather than requested.*
- The round-1 → round-9 distance is the measurable growth: "is it my index builder?" → five design-layer questions answered from standing doctrine the same night. Rounds 4 and 9 are also expressed as `kh:ElicitationRound` instances (the class your five-beat evidence finally earned).

## 3. Worked example (shorter): state-vs-ephemera, and what "decision" now means

Your hunch-challenge is the arc's pivot — *"I'm treating predicted Transmitter existence as ephemera, a dynamic artefact of a run"* — and everything downstream (frames-boundary rule, doc 14, the 30M-row production run, the pre-build layering doctrine of round 9) is now expressed as executions of an understanding fixed in that exchange.

Two v2 mechanisms show up here concretely:

- **`kh:showsFormationOf`** — the transcript ADDITION of the 1am argument (21ce14af) now points at the tranche-1 outcome episode (616377e6): *the target holds the doctrine, the source holds the birth.* Thirty-three of the forty-one previously-untyped prose cross-references turned out to be exactly this relation, which is the typing pass's main empirical finding: the ADDITIONs really were "the understanding-arcs trying to exist without a type."
- **`kh:frameAdopted`** — your reframe of decision-as-frame-adoption, encoded. `khe:frames-boundary-rule` now carries: *"Populations live in regenerable frames; descriptions and rare promoted exemplars live in the map."* `khe:workbench-repo-reversal` carries: *"Human legibility is part of the project's epistemics, not its tooling."* The decision is the dated accountable act; the frame is the way-of-seeing it commits to, held weakly. `kh:decision`'s own comment now says this, with the v1 wording retained verbatim inside it.

---

## 4. What changed, at one altitude up

1. **Center of gravity: decision → understanding.** kh:Episode and kh:decision reframed (supersede-retain; v1 comments quoted verbatim in their successors). Decisions stay the datable anchors — as frame-adoptions with accountability, your formulation — and the growth of understanding gets its own first-class carrier: the arc.
2. **kh:Arc ⊑ rc:Pattern**, dual-typed, membership by rc:supportingObservation, ordering by kh:leadsTo (80 edges, DAG — shared members braid arcs together), pivotQuote + becameAutomatic mandatory (SHACL), onsetMember/onsetShape where the record supports them. All 13 arcs expressed. A parallel arc machinery was refused (t5-17 precedent); a strict list ordering was refused (your graph insight).
3. **The 41-edge debt paid.** Every EPISODE-ADDITION cross-reference typed: 33 showsFormationOf, 4 paysOff, 1 continues, 1 commissionedBy, 2 generic (prose ambiguous — generic is honest), plus 7 secondary edges where prose names a second anchor. kh:relatesToEpisode generalized to the observation layer (v1 domain/range removed, retained in the comment).
4. **Onset repair.** Two new onset observations recorded from the archive (the Jan-1 anomaly 5b7eba52; your round-1 reply 4f51b4bf). Five onset kinds cleared ≥2 among the 13 arcs: anomaly-registered, question-arrives, expert-reframe-received, cases-accumulate, prior-formation-surfaces (that last one is itself a finding: some arcs begin with understanding that formed off-record). The maps arc's onset is honestly marked as the *in-corpus arrival* of a ten-year-old wish; the transcripts arc's onset kind (limit-predicted) sits at n=1 and stays parked.
5. **Couldn't-say ledger worked** (promotions and refusals in §6/§7).
6. **kh:Heuristic and its promotion gate: untouched.** Zero instances, exactly as before; the docket (synthesis metas 4/5) remains yours.

## 5. The v2 terms, one line each

**Classes**: `kh:Arc` (above); `kh:OnsetShape` (arc-beginning kinds, own ≥2-citation SHACL); `kh:DelegationContract` (promoted at 5+9+6 instances; ghostwriter + external-maintainer expressed, terms quoted); `kh:ElicitationRound` (v1's "digest only" objection retired by rounds 1-10 as live episodes; rounds 4 and 9 expressed); `kh:AttentionProtocol` (≥5 designed interface rules; ranked-queue + pop-in-digest expressed).

**Episode shapes**: `kh:interruptionResume` (3 distinct incidents, 3 distinct recovery routes; single-window caveat kept on the term); `kh:selfCorrectionConfessed` (≥5 instances spanning tiers; confession is what gets praised into the record).

**Onset shapes**: `kh:anomalyRegistered`, `kh:questionArrives`, `kh:expertReframeReceived`, `kh:casesAccumulate`, `kh:priorFormationSurfaces` — each ≥2-cited from the 13 arcs.

**Relations** (subproperties of the generalized kh:relatesToEpisode): `kh:showsFormationOf` (doctrine↔birth, the dominant one), `kh:paysOff`, `kh:continues`, `kh:commissionedBy`, `kh:leadsTo` (arc ordering).

**Properties**: `kh:pivotQuote`, `kh:becameAutomatic`, `kh:onsetMember`, `kh:onsetShape` (arc); `kh:frameAdopted` (your reframe, instance-judged), `kh:correctedInFlight` (≥4 wrong-move-then-correction devices), `kh:consentBasis` (the consent ladder quoted, not enumerated — an enum was refused because the rungs are utterances, not categories; expressed on the two hold episodes), `kh:conversionLatency` (recorded phrases, not fabricated durations; awaiting map instances, the v1 sealedCriterion precedent); `kh:contractTerm`, `kh:underContract`.

**Changed (supersede-retain)**: kh:Episode, kh:decision, kh:relatesToEpisode. Nothing deleted anywhere; the five removed triples (three v1 comments, one domain/range pair) are quoted verbatim in their successors.

## 6. Refusals and parkings (couldn't-say v3) — as valuable as the terms

1. **kh:corrects / kh:confesses as relations — refused.** The metas' counts describe episode *content* (confession events), not record-to-record relations; among the 41 edges neither occurs once. Confession content lives in the selfCorrectionConfessed shape.
2. **Attention-model person-terms (burst-reviewer, review-surface, folding-phone…) — parked deliberately.** The ≥6 instances exist, but a capsule modelling *its owner's* attention should be an owner-reviewed act, not a distiller's. Your call to make, with the evidence listed.
3. **Context-inversion / rationale-voided-by-environment — parked.** Two instances, but they are the two halves of one deadline saga; a second independent inversion promotes it.
4. **Absence-by-request — parked** (n=1 plus a different-form second).
5. **Outcome-status enum — parked again**, now with its case set named (rescoped-pre-build; reversed-with-purpose-retained; pin-with-migration-pending; superseded-by-own-review).
6. **Structured sighting-counts — refused, reason recorded**: the forms are heterogeneous ("3 sightings", "4 sessions", "twice today", the ≥2 rule itself); one numeric field would flatten meaning; deferralTrigger prose wins.
7. **Protocol-note-vs-break — parked** (2 instances inside a single trial).
8. **ElicitationRound beat-properties — parked** until all ten rounds are expressed (archivist work).
9. **Contract term-kind taxonomy — parked** (terms vary too much in form to distill kinds).
10. **limit-predicted as onset shape — parked** at n=1 among arc onsets.
11. **Strict-sequence arc ordering (rdf:List) — refused** per your "one leap powers many."
12. **Parallel arc-capture machinery — refused** (t5-17: extend the existing form).
13. **The skill terminus** (judgement-test invariants / realizations / parameters) — still deliberately unbuilt; **heuristic promotion — still unexercised**; the docket is yours.
14. **Span anchoring** — still belongs to the SourceSpan machinery, not kh:.
15. **The interruption-resume ARC** (as distinct from the shape) — still unrecorded pending cross-window evidence.

## 7. The gate proven, and one honest slip

A deliberately invalid arc (no pivotQuote, no becameAutomatic) was staged as a negative probe: staging validation flagged 2 MinCount violations, apply refused (`can_apply: false, validation_failed`), the graph was never touched, and the probe was closed discarded with its rationale kept — the v1 under-cited-heuristic precedent repeated for v2. **The slip**: a verification command re-invoked the staging call instead of reading its saved output, creating a duplicate probe row — the exact inspection-re-run slip HARVEST-5 documented. Both rows are closed discarded with the slip recorded in the rationale. The trap is now twice-sighted; by the corpus's own counting rule that earns the cheap fix (a read-only stage-inspection route or idempotency key) a product-ledger entry.

## 8. Verification state

validate_graph scope "all": conforms, 0 results (checked after each of the three revisions and at close). Zero staged debt (plan_staged_revision_recovery: 0 rows; four closed rows this session: 3 applied, 2 probe-discards). export_preflight: block-gate unchanged — 16 match rows across the SAME 4 pre-existing tranche-3 repair-residue resources HARVEST-5 documented; nothing from this session fired. Retrieval sanity: arcs, onset episodes, new terms, and contract instances all retrieve first on their natural queries (one arc query needed sharpening — logged in DISTILL-2 with the friction note about history patch rows outranking live nodes). Every cited IRI was verified against the capsule's own listed observations before staging; six citation heads from a predecessor journal matched nothing in the graph and were re-resolved from the capsule — graph over journals, again.
