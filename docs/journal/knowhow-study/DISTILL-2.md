# DISTILL-2 — Vocabulary distiller journal (know-how pilot, tranche 2 — owner-greenlit revision)

**Date**: 2026-08-09. **Role**: VOCABULARY DISTILLER tranche 2, commissioned by the owner's two 2026-08-09 reviews (27da9e10 understanding-lens critique; 93587901 arc-analysis endorsement: decision = frame-adoption + accountability, held weakly; arcs as graph elements; greenlight for the vocabulary pass with worked examples).
**Capsule**: /home/codex/knowhow-study/capsule.sqlite via `venv/bin/python bridge.py call doxabase.<tool>`. Start state: 297 observations / 297 evidence, ontology 250 (kh: v1), shapes 95, map 315, patterns 0; zero staged debt.
**Read first, in order**: owner reviews 27da9e10 + 93587901 (capsule); ARCS-1.md + 13 arc payloads + 3 arc metas; DISTILL-1.md + VOCAB-NOTE-KH1.md; all five harvest journals' meta-observations (t1 m1-m4, t2 m1-m5, t3 m1-m5, t4 m1-m5, t5 m1-m8).

## Charter (the owner-ratified mandate)
1. Center of gravity decision → understanding; decisions stay as datable anchors reframed as FRAME-ADOPTIONS with accountability ("a stance on how things should be seen... held weakly"). Encode in kh:decision semantics; judge kh:frameAdopted from instances.
2. kh:Arc ⊑ rc:Pattern; graph-shaped ordered membership; pivotQuote (13/13); becameAutomatic (13/13); all 13 arcs expressed as instances.
3. Type the 41 prose ADDITION cross-references with distilled v2 relations; generic where ambiguous.
4. Onset repair: record missing arc onsets as new episodes from archive chunks; onset-shapes with ≥2 instances.
5. Work the consolidated couldn't-say ledger, ≥2 rule; refusals recorded with reasons.
6. Supersede-retain for v1 changes; kh:Heuristic + gate untouched (zero instances; docket remains the owner's).
7. VOCAB-NOTE-KH2.md leading with worked examples (owner's style law).
8. validate_graph scope=all conforms; zero staged debt; graph over journals.

## Log
- Read in mandated order. Both owner-review observations retrieved by search (27da9e10, 93587901); 13 arc payloads + 3 metas read from payloads-arcs/ (IRIs verified against iri_map.txt); DISTILL-1/VOCAB-NOTE-KH1 (v1 decisions, couldn't-say §5); five harvest journals + all 27 meta payloads read.
- The 41 EPISODE-ADDITIONs identified by marker prefix in payload mirrors: exactly 41 (t2: 2, t3: 12, t4: 15, t5: 12 — the t2/t3 journal counts differ from the marker census; marker census matches meta-t5-m1's 41; graph/marker over journals).
- Full observation IRI list pulled from the capsule via list_entities (297, no 8-hex head collisions); all anchor heads resolved against it. JOURNAL-HEAD FAILURES (graph over journals, again): HARVEST-4's overlap-sweep heads 9eaa6a5a / a1fcc9ec / be5572e8 / 999510de / 391be9db / 79a6d088 match NO capsule IRI (not as head, not as substring). Real anchors resolved by search + arc cross-reference instead: Voyage t1 = 846c1b47; doc-14 t1 = f363122e; doc-13 t1 = b5ddf626; production-run t1 = f4c5b61d; separate-repo t1 = b4f0f022; baseline-trial t1 = efebac72 (ep-a04). 0d2aa1ca and similar are mid-IRI fragments (resolved by substring: ghostwriter contract = c0ba4746-...-0d2aa1ca9b1a).
- rc:Pattern machinery read in the live wheel (rc_core.ttl / rc_shapes.ttl): rc:supportingObservation is the membership predicate — REUSED for arc membership per the corpus's own micro-method 91d07d34 (read the term before minting). PatternShape requires exactly-one rc:summary/patternText/rationale + ≥1 patternTarget + the sh:or evidence clause; arcs will be dual-typed kh:Arc + rc:Pattern and satisfy all of it (subclass targeting not relied on).
- stage_revision kind='graph' spec fields discovered by the sanctioned targeted error: required [rationale, summary]; optional includes additions AND removals (removals support confirmed — needed for supersede-retain comment replacement).
- Onset sources verified verbatim in the archive: Jan-1 anomaly = chunk-0018 L2728 ("2,736 of 2,738 name transitions land exactly on Jan 1 — it's claiming your index builder canonicalizes identity annually"); round-1 owner reply = chunk-0019 L2731 ("I'm surprised static identity is frozen per year... or is a bug in my index file generation... I am confused by this"), which also carries the round-1 tradecraft volunteering (the shared-MMSI segregator story) that c78a2ac2 says returns in round 9.

## The 41-edge sort (done before minting subrelations, per meta-t5-m7 item 1 / meta-arc-3 prerequisite)
Each ADDITION's prose read in full; primary edge typed; secondary edges only where the prose names a second anchor. Tally (primary edges): showsFormationOf 33, paysOff 4 (t4-30, t4-42, t5-01, t5-10), continues 1 (t4-49), commissionedBy 1 (t3-48), generic relatesToEpisode 2 (t3-34, t5-16 — prose ambiguous; generic is honest). Secondary edges 7 (5 showsFormationOf, 1 continues, 1 generic). Subrelations minted only where corpus-wide justification reaches ≥2 (continues and commissionedBy clear via map edges + meta-t3-m4's counted burst→session instances). REFUSED as relations: corrects / confesses — the metas' counts describe episode CONTENT (confession events), not record-to-record relations; among the 41 edges neither occurs even once. Confession content goes to the selfCorrectionConfessed shape instead.

## Onset census (the 13 arcs)
Two onsets missing AND archive-recoverable → recorded as new observations by this session: onset-arc01 = 5b7eba52 (Jan-1 anomaly registered, chunk-0018 L2728, verified verbatim) and onset-arc09 = 4f51b4bf (round-1 puzzled reply, chunk-0019 L2731, verified verbatim; also carries the round-1 segregator tradecraft that returns in round 9). Eleven arcs' onsets are their earliest members, classified: anomalyRegistered (arc-01 via new obs, arc-07), casesAccumulate (arc-02, arc-12), expertReframeReceived (arc-03, arc-06), questionArrives (arc-04 — the ten-year wish's IN-CORPUS arrival, formation off-channel honestly noted; arc-09 via new obs; arc-13), priorFormationSurfaces (arc-08, arc-10, arc-11 — itself a finding: some understanding arrives pre-formed). arc-05's kind (limit-predicted) at n=1 among onsets → parked, arc left without onsetShape (comment explains).

## Execution record (2026-08-09)
- Onsets recorded first (payloads-v2/onset-arc0{1,9}.json → 5b7eba52, 4f51b4bf; duplicate-checked by search before recording; observations 297 → 299).
- All Turtle BUILT WITH RDFLIB programmatically (payloads-v2/gen_v2.py + arcs_data.json + edges_data.json), no string templating of literals — the t4 escaping lesson made structural. Every cited IRI asserted against the capsule's listed observation set inside the generator; serialization re-parse-checked. One near-miss caught by this discipline: three arc patternTargets were first written with GUESSED full UUIDs; replaced with head-resolution against the capsule list before anything was staged.
- Supersede-retain removals verified against the LIVE graph before staging: all five removal triples (3 v1 comments + relatesToEpisode domain/range) confirmed verbatim via describe_resource diff.
- REVISION A (vocabulary: ontology +197/-5, shapes +132): staged b12dd772 (preview conforms) → apply dry_run (can_apply, ready) → applied 5dace5e7 → validate all conforms.
- REVISION B (arcs + relations: patterns +390): staged 2cb9f92d (preview conforms) → dry_run ok → applied 02897ed6 → conforms. Patterns graph content: 13 kh:Arc (dual-typed rc:Pattern; rc:supportingObservation membership; pivotQuote/becameAutomatic 13/13; onsetMember 13; onsetShape 12), kh:leadsTo 80 (deduped DAG), typed edges 48 (38 F + 4 P + 2 C + 1 CB + 3 G including secondaries).
- REVISION C (map instances +55): staged 552815fd (preview conforms) → dry_run ok → applied 62f55823 → conforms. Map now: 37 kh:Episode (35 + khe:seealso-workaround-filed, khe:fresh-claim-not-retroactive — the labelledWorkaround typing debt paid), consentBasis on 2 episodes, frameAdopted on 2, 2 DelegationContract, 2 ElicitationRound, 2 AttentionProtocol instances.
- NEGATIVE PROBE (the gate must bite): pivot-less arc staged → validation preview 2 MinCount violations (pivotQuote, becameAutomatic) → apply dry_run REFUSED (can_apply false, validation_failed) → closed discarded with rationale. SLIP, honestly recorded: the probe's verification command re-invoked stage_revision with the same @file instead of reading saved output → duplicate probe row c68a7691 — the EXACT inspection-re-run slip HARVEST-5 documented (second sighting; earns the cheap fix per the corpus's own counting rule). Both rows closed via stage_revision kind='review_decision' decision='discarded' allow_mutation_target=true.
- END STATE: validate_graph scope=all conforms True 0 results; plan_staged_revision_recovery current_staged_work_only → 0 rows; project_brief 299 observations / 299 evidence / 13 patterns. export_preflight: 16 rows across the SAME 4 pre-existing tranche-3 residue resources (77d54527 / a4d70dfe / ce476bd0 / 84889151 snapshot) — nothing from this session fired; row-count basis still varies per export_part (HARVEST-5 friction 5 stands).
- Retrieval sanity: "frame-adoption with accountability held weakly" → 93587901 then kh:Episode/kh:decision; "interruption resume resource exhaustion" → kh:interruptionResume first; "the Jan-1 identity anomaly registered onset" → kh:anomalyRegistered + 5b7eba52; "delegation contract external maintainer invariants" → kh:DelegationContract + fd36f1a7; arcs: "anomaly-to-instrument chain Jan-1 public centerpiece" → kha:identity-freeze first (the looser "understanding arc identity freeze pivot quote" surfaced a history graph-patch row first — sharper phrasing needed, both logged; see friction).
- Deliverables: VOCAB-NOTE-KH2.md written (worked examples first per the owner's style law: identity-freeze end-to-end with the new onset, elicitation-loop round-1→round-9, state-vs-ephemera with frameAdopted/showsFormationOf; then terms, refusals, gate proof, verification).

## Friction (for the product ledger)
1. stage_revision kind='graph' REMOVALS confirmed supported and working (first session to exercise them): [{"graph":..., "content": <turtle>}] mirror of additions; surgical supersede-retain replacement landed exactly (-5 verified by describe_resource after apply).
2. A validation-failing graph payload STAGES as a failed candidate row (validation_conforms false) rather than being refused at stage time; apply then refuses. Net safety identical to DISTILL-1's account but the row needs review_decision closure — worth one doc line so distillers expect the cleanup step.
3. Inspection-re-run slip trap, SECOND sighting (HARVEST-5 friction 1 territory): re-invoking stage_revision with @file to "look at" a result re-stages it. A read-only describe of the just-created row is describe_revision — the habit to teach; an idempotency key would remove the trap class.
4. Journal citation heads can be wrong wholesale: six of HARVEST-4's overlap-sweep heads (9eaa6a5a, a1fcc9ec, be5572e8, 999510de, 391be9db, 79a6d088) match NOTHING in the capsule (not as head, not as substring); the anchors were re-resolved by search. Graph over journals, fourth independent sighting — citation heads in journals should be spot-checked against the graph before reuse.
5. search scope='graphs' ranks history graph-patch rows (which embed whole Turtle payloads) above live nodes for some queries; excluding patch literals from default ranking (or a graph filter param) would keep retrieval pointed at current knowledge.
6. list_entities paging at limit=100 over 299 observations: clean, no collisions among 8-hex heads — the head-resolution workflow (list once, resolve locally) is cheap and reliable; recommend as standing practice over per-head searches.
