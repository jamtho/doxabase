# DISTILL-4 — Vocabulary distiller journal (know-how pilot, hygiene pass 4 — the terminus-surface generalization)

**Date**: 2026-08-15. **Role**: DISTILLER-4, a small vocabulary-hygiene pass working the seeds
the last week's runs accumulated — NOT a major version. Scope fixed by brief: (1) the
terminus-surface generalization the Polymath sealed benchmark's judge demanded, (2) the
count-refresh Revival 2 parked, (3) the product-ledger consolidation into one
distiller-batch-2 work order, (4) couldn't-say v7.
**Capsule**: /home/codex/knowhow-study/capsule.sqlite via `venv/bin/python bridge.py call
doxabase.<tool>` (DISTILL-3 mechanics). Foreign capsules — enron, syzbot, gerrit, and now
**polymath** (/home/codex/polymath-study/capsule.sqlite) — READ-ONLY throughout, verified
against their graphs directly (read-only sqlite + rdflib), never trusted from journals;
mtimes recorded at open and re-checked unchanged at close.

## Start state (verified via bridge before any mutation)

334 observations / 334 evidence / 13 patterns; validate_graph scope=all conforms, 0 results;
recovery planner 0 rows — zero staged debt. Two observations newer than Revival 2's close:
the Polymath verdict (c81248ac, PASS with the terminus-surface couldn't-say) and the
2026-08-15 owner round (2c509405 — "incredibly promising," next step deferred to the
curator). The couldn't-say that matters, from the judge report verbatim: the SEALED
PREDICTION failed because all nine polymath arcs carry kh:silentTerminus — "the seeded
vocabulary documents kh:narratedTerminus as syzbot-bot-specific, so a dialect-conforming
ingester could not legally mark the announcement narrated."

## Graph verification log (all counts re-verified 2026-08-15, never journal-trusted)

- **Home**: 13 arcs, all typed silentTerminus. narratedTerminus comment stale exactly as
  Revival 2 flagged ("4 verified instances, all syzbot arcs"; observedInCorpus syzbot only).
- **Syzbot**: 5 arcs, NO terminusSurface edges (pre-v3 capsule); the 4 narrated / 1 silent
  classification re-verified from the arcs' own becameAutomatic texts (patchwork-bot notice /
  b4-ty / tip-bot / templated apply notice; hfs dissolves into agreement).
- **Enron**: 1 arc (vitro), no surface edges — comment-carried silent.
- **Gerrit**: 2 narrated + 3 silent, TYPED. The find of the pass: octavia-ai-review-
  renegotiation's basis reads "Participant-stated: the operator narrates the close twice" —
  a HUMAN narration already typed narratedTerminus under v3. The bot-scoping lived in
  comment prose, never in graph law; only comment-obeying ingesters (polymath's, by design)
  were bound by it.
- **Polymath**: 394 obs / 394 evidence / 192 claims; 26 kh:Episode (filed in its
  observations graph — its own convention); 9 arcs in patterns, all silentTerminus; 3
  DeclinedOption; 64 leadsTo edges; harvested on the PRE-revival-2 v3 seed (no
  kh:terminusReversal, no khcap:gerrit-knowhow); self-registered khcap:polymath-knowhow +
  15 observedInCorpus edges; seed sits in ROLE graphs (unlike the 3 URN-filed capsules —
  see the work order); conforms on a scratch copy, zero staged debt.
- **The announcement's true position** (the "verify in the polymath graph first"
  instruction, and it mattered): the 'Problem solved (probably)' observation ef38d698 is
  the TERMINUS MEMBER of arc proof-strategy-collapse-to-breakthrough (its own synthesis
  observation c68dfa37: the arc "shares its terminus member with the establishment-standard
  arc") and the ONSET member of establishment-verification-standard. So the arc that gets
  the narrated re-expression is PROOF-STRATEGY, not establishment — the establishment arc's
  completion (solvedness stops being asked) genuinely is silent, its narration signal
  sitting at onset/pivot with participant-stated honestly in basis prose. Reclassification
  boundary recorded in the dossier; only ONE of nine polymath silent edges is a v3-scoping
  artifact.
- **Surface/basis axis separation** (the verification's key finding): the foreign
  proof-strategy basis prose — "Completion here is behavioural, not a formal announcement
  of the strategy question's closure" — conflates the two axes. The BASIS is continuation
  (correct; narration proves application, never automaticity — standing law). The SURFACE
  is narrated: the Mar 9 'in the bag' declaration and the Mar 10 announcement post are
  completion-narration events in the record. v3 gave the ingester no way to say both.

## Design decisions (one line each)

1. **Generalization by supersede-retain, no new terms**: narratedTerminus = narration by
   ANY participant or mechanism (bot notice, merge message, operator close, announcement
   post, ritual thread-close); silentTerminus = no narration event, completion confirmed
   externally by what stops happening (the becameAutomatic prose carries the continuation);
   TerminusSurface = the surface belongs to the corpus's RECORD (tooling, conventions,
   narration habits). All three v3 comments retained verbatim inside the new ones.
2. **Counts refreshed against live graphs**: narrated 7 events (syzbot 4 + gerrit 2 +
   polymath 1); silent 26 (13 home + 3 gerrit + 8 polymath typed + enron vitro + syzbot hfs
   comment-carried). observedInCorpus on both individuals extended +gerrit +polymath.
3. **Three re-expressions, established mechanism** (registry → dossier → per-witness
   evidence → re-express): the proof-strategy ARC re-expressed home with narratedTerminus
   (foreign edge stays silent under the frozen seed — divergence recorded on the evidence
   observation; graph-over-journals extended to graph-over-frozen-seed); the thread-16
   numbering-cap ritual close (human narration MECHANISM, episode, deliberately not counted
   in the arc-surface census); the write-up-thread fade (the silent contrast the polymath
   record itself draws — "contrasting with every prior mainline thread's numbering-cap
   handoff ritual").
4. **Couldn't-say re-grade**: resolved-by-revision for expressibility; the sealed
   prediction itself STAYS FAILED — the seal graded the frozen v3 output and nine silent
   arcs is what a dialect-conforming ingester legally produced. History is history.
5. **Refusals recorded** (v7): no polymath dialect shape (one corpus's convention, below
   the bar); terminusBasis count refresh REFUSED this pass — case (ii) participant-stated
   is demonstrably n=3 now (syzbot machine-authorship, gerrit octavia, polymath
   establishment) and case (i)'s "the gerrit corpus's five arcs continue the pattern"
   overcounts by the octavia exception, but a refresh would double-nest a supersede-
   retention on a term already carrying one; correct counts recorded in the dossier and v7,
   refresh parked for the next major pass.

## Execution log (2026-08-15)

- Polymath conformance checked on a scratch COPY (conforms, 0 results; recovery 0 rows);
  all four foreign mtimes recorded (payloads-distill4/foreign-mtimes-open.txt).
- **Seven pre-staging observations** (payloads-distill4/, IRIs in obs_iris.json; every
  foreign quote pulled character-for-character by gen_obs.py with inline asserts):
  registry 3f617f5f (polymath); v4 terminus dossier 15020420 (five-corpus census, counting
  rules, reclassification boundary, the octavia find, the terminusBasis stale-count
  discovery); witness evidence b1dfdd20 (announcement), a57bba91 ('in the bag'), c666d212
  (thread-16 ritual close), 7b4d0fac (write-up-thread fade); product-batch meta 52dc4a8e.
  Observations 334 → 341.
- All Turtle BUILT WITH RDFLIB (gen_v4.py, gen_r1b.py): cited IRIs asserted against the
  capsule's 341 stored observation subjects; arc/episode-name collision checks; all three
  supersede-retain removal triples verified verbatim-equal against the LIVE graph inside
  the generator; serializations re-parse-checked.
- **R1 (ontology +17/-3)**: staged e061e169 (preview conforms 0) → dry_run ready → applied
  8a1659ba → validate all conforms. Contents: khcap:polymath-knowhow (same IRI the capsule
  self-registered — one identity, two anchors, the gerrit precedent); the three
  supersede-retained comments; seeAlso to dossier + registry; 4 observedInCorpus edges.
- **R2 first staging FAILED — the gate teaching, not failing**: preview validation flagged
  exactly 2 violations on the re-expressed arc (EpisodeNodeShape decision/fromObservation
  minCounts). Root cause: kh:reexpressedFrom carried rdfs:domain kh:Episode, so RDFS
  inference typed the ARC an Episode. The v5 couldn't-say parking anticipated exactly this
  widening ("sourceCapsule is deliberately domain-less to receive" future carriers) — the
  arc carrier arrived before the claims/person carriers. Row e906fc70 closed
  kind='review_decision' decision='discarded', rationale kept.
- **R1b (ontology +2/-2, gate-taught)**: staged c2d60b0a (conforms) → applied 4759dc12.
  rdfs:domain removed (reexpressedFrom now domain-less, symmetric with sourceCapsule);
  comment supersede-retained (nested retention accepted as the honest cost — the revival-2
  comment, itself retaining v3, kept verbatim). ReexpressionShape's pairing gate is
  class-independent and unchanged — it targeted the arc correctly throughout.
- **R2 (patterns +19, map +15)**: restaged fresh 67305189 (preview conforms 0) → dry_run
  ready → applied 7df6721c → validate all conforms. Contents: home
  arc/polymath-proof-strategy-collapse-to-breakthrough (kh:Arc + rc:Pattern; foreign
  pivotQuote/becameAutomatic/patternText/summary verbatim; terminusSurface narratedTerminus;
  terminusBasis carrying the foreign basis verbatim + the v4 surface/basis separation;
  onsetMember deliberately absent, stated in the rationale — the re-expression carries only
  terminus-relevant members); episode/polymath-thread16-numbering-cap-close;
  episode/polymath-writeup-thread-fade (no rationale — the record holds no stated reason
  for the lapse; never invent rationale).
- **Session meta + couldn't-say v7**: observation 0d8a0bb5. Observations 341 → 342.
- **Seed re-export (freeze)**: kh-vocab-seed.trig regenerated from the live ontology (753)
  + shapes (427) graphs → TriG, 1180 quads, re-parse-checked; previous 1166-quad seed kept
  at payloads-distill4/kh-vocab-seed.trig.pre-v4.bak.
- **END STATE**: validate_graph scope=all conforms, 0 results; zero staged debt (recovery
  planner 0 rows; 4 rows this session: 3 applies + 1 preview-caught discard); 342
  observations / 342 evidence; ontology 753 / shapes 427 / map 818 / patterns 423 quads;
  63 kh:Episode, 14 kh:Arc (home narrated-typed n=1), 5 kh:Capsule, 11 kh:Heuristic, 13
  kh:DeclinedOption, 7 kh:PolicyStatement. export_preflight: the SAME 4 pre-existing
  tranche-3 residues (77d54527 / 84889151 / a4d70dfe / ce476bd0); NOTHING from this session
  fired. Foreign capsules verified untouched at close (mtimes diff-clean).

## The product-batch meta (distiller-batch-2 work order)

Observation **52dc4a8e**, one consolidated meta, five items, journals cited and live-verified
where checkable:
1. **record_pattern/record_observation cannot express kh: extension fields** — hand-authored
   Turtle/TriG in 5 capsules (enron/syzbot/gerrit pilots, polymath INGEST-1 sessions 1-8 +
   synthesis, home ARCS-1); plus the thrice-earned ArcShape note (the seed's ArcShape never
   mentions the rc:Pattern minCounts dual-typing pulls in).
2. **export_preflight blind to typed sensitive categories** (khpm:/khperson:, khpol:/khcap:,
   khgp:/khgpm:) — 4+ sightings (PROMOTE-1 f4, DISTILL-3 f3, GERRIT-PILOT-1 f3, re-confirmed
   Revival 2 + this pass); correct today only because the block gate holds everything.
3. **History rows outrank live nodes in search** — SEVEN prior sightings consolidated
   (DISTILL-2 f5; SYZBOT-PILOT-1 f1; PROMOTE-1 f1; DISTILL-3 f1 ×2; Revival-2 f2;
   GERRIT-PILOT-1 f2), and this session added the EIGHTH and NINTH ("polymath capsule
   registry" and "problem solved probably announcement" both rank history rows above live
   nodes; positive: kh:narratedTerminus ranks FIRST for its natural query). Fix shape
   unchanged: graph-role filter + patch-literal downranking.
4. **import_bundle files imported graphs under URN names invisible to list_entities** —
   verified live in exactly 3 capsules (enron/syzbot: urn:doxabase:graph:ontology 473 +
   shapes 258; gerrit: 724/427); polymath (newer wheel) filed the same seed into ROLE
   graphs — the cross-version inconsistency is itself part of the bug surface.
5. **export_bundle(kind='context_slice') prefix/wrapper instability** — 2 sightings
   (INGEST-1 sessions 4 and 8: @prefix lines omitted vs included; stable half: the
   rcg:<role> block wrapper is never accepted by stage_revision content and must always be
   stripped).

## Friction (this session's own, beyond the consolidated items)

1. History-outranking sightings 8 and 9, logged above — filed into the work order rather
   than re-argued.
2. **Positive**: the staging preview caught the reexpressedFrom domain limit BEFORE any
   graph damage — a real (undeliberate) negative proving the same gate the v3 probe proved
   deliberately. The two-violation report named the exact shape and paths; root-cause to
   fix was one query (the rdfs:domain triple).
3. **Positive**: the register → dossier → re-express → supersede-retain pipeline ran its
   third exercise with zero mechanical discovery errors — the journals' accumulated schema
   notes have fully amortized; the only failed staging this session was semantic (the
   domain), not mechanical.
4. Note for future harvesters: the polymath capsule files kh:Episode instances in its
   OBSERVATIONS graph (not map) — a per-capsule filing difference worth knowing before
   counting anything.

## Final verification sweep

validate_graph scope=all: conforms, 0 results. Recovery planner: 0 rows. project_brief:
342/342, patterns 14, shapes 66. Foreign capsules: polymath 71,073,792 bytes / mtime
2026-08-13 04:14, enron + syzbot mtime 2026-08-11, gerrit mtime 2026-08-13 01:35 — all
diff-identical to the open snapshot, all opened mode=ro only. Seed 1180 quads,
re-parse-checked. VOCAB-NOTE-KH3 §7 carries a v4 addendum (both copies, home + repo).
