# PROMOTE-1 — Promotion distiller journal (know-how pilot: first exercise of the promotion gate + the consented person-model)

**Date**: 2026-08-11 (session start late 2026-08-10). **Role**: PROMOTION DISTILLER, the owner-gated session, unlocked by the two 2026-08-11 owner-review observations:
- `a37f1c65` — "absolutely fantastic so far... extremely promising"; limitation hypothesis = source scarcity; three remedies offered (fresh product work, second domain, a friend's corpus).
- `813ba95e` — gates passed: "keep going down this road" read with the standing delegation as authorization to exercise the promotion gate on the ten-candidate docket (the SHACL >=2-citation gate stays the mechanical protection); on the parked person-model, "why would I say no?" = informed consent, WITH conditions: private capsule only, nothing person-modelling goes public, every claim carries assessedAt/data-window indexicality.

**Capsule start state**: 301 observations / 301 evidence / 13 patterns / ontology 442 / shapes 227 / map 370; validate_graph scope=all conforms; zero staged debt. Backup taken (scratchpad, pre-promotion).

## Charter
1. PART 1 — first-ever exercise of the kh:Heuristic promotion gate: the docket's ten candidates (synthesis metas 4+5 = obs 0d5f6b18 / 1072586d), each with one behaviouralStatement (imperative sentence), applicabilityConditions where episodes show scope limits, knownFailureModes where the corpus records failure/misapplication, citesEpisode >= 2 (SATISFY the gate, never weaken it), promotedFromShape where a shape matches. Citations verified against the GRAPH before staging (the tranche-5 law: graph over journals). Near-bar three stay unpromoted unless evidence now suffices — refusals recorded with reasons.
2. PART 2 — the person-model, owner-consented with conditions: distill 4-8 terms from the >=6 recorded attention instances; claims-about-observed-behaviour with evidence + assessedAt/assessmentWindow indexicality mirrored in kh: (aisv: NOT imported); never essence.
3. PART 3 — couldn't-say ledger v4; note what the second-pair corpus will stress-test.
4. validate_graph scope=all conforms; zero staged debt at close.

## The gate, read from the graph before anything was staged
`khs:HeuristicShape` (shapes graph, verified via direct read): targetClass kh:Heuristic; exactly one `kh:behaviouralStatement` (xsd:string); `kh:citesEpisode` minCount 2, nodeKind IRI, **sh:class kh:Episode** — every citation must be a typed kh:Episode, not a raw observation. This is the load-bearing discovery of the session: the docket cites a mix of khe: episodes and observation IRIs, so satisfying the gate honestly requires the cited observations to be EXPRESSED as map episodes first (each anchored by kh:fromObservation — auditable re-expression, the same typing-debt payment DISTILL-2 made for labelledWorkaround). Weakening options (editing the shape, dual-typing observations across layers) refused.

## Citation resolution (graph over journals, executed)
All docket citing IRIs resolved against the capsule's 301 listed observations — **zero dead heads** (the docket's own verification held, unlike HARVEST-4's six phantom heads). Suffix/mid-fragment forms resolved: b84f4ef7d1e1→0102b002-..., b63e9632(head), c31d2431468a→b2fd3846-... (= khe:loop-architecture-from-owner's fromObservation — semantic match verified by full-text read), 8ab1b702f965→08e1783a-..., 79621c60(head), 711926e339c5→9df11f66-..., 597c91acf1ae→c21a015a-..., 0d2aa1ca→c0ba4746-... (mid-fragment, as DISTILL-2 recorded), 14340bf481c6→46671b8b-..., f0fb66e8(head), 8dcf2631(head). Existing-episode coverage checked via kh:fromObservation inverse map: only 2 of the docket's observation citations already had episodes (loop-architecture-from-owner, weight-without-information); 18 lacked them.

## Plan of record
- R1 (map): 18 new kh:Episode instances re-expressing the docket-cited observations, each with exactly-one decision, fromObservation anchor, rationale/outcome from the record only, hasShape where an EpisodeShape individual genuinely matches (decisionWithFalsificationTest, deferralWithTrigger, lawByDemonstration, honestNegative, expertCorrectionAdopted).
- R2 (map): the 10 kh:Heuristic instances + 3 kh:DeclinedOption graveyard entries for the near-bar refusals (practicing candidate 9 on the promotion act itself).
- R3 (ontology+shapes+map): person-model vocabulary (kh:PersonModelClaim, aboutPerson, observedBehaviour, assessedAt, assessmentWindow, claimEvidence + PersonModelClaimShape with claimEvidence minCount 2), khperson:james, 7 claims; supersede-retain of kh:AttentionProtocol's v2 parking comment (the parking is discharged by the owner review it asked for, not overruled).
- Then: session observation (promotion meta + couldn't-say v4) via record_observation; final verification sweep.

## Near-bar refusals (reasons, pre-stated before staging)
1. **tests-encode-semantics — NOT promoted.** Two of its three citations record the SAME wave-R1 batch-regex incident (dfe60be7 = the doctrine record; aaa8fdd3 = khe:batch-regex-overreach-confessed's live original — verified by full-text read). Independent-event count is 2 adjacent operator-side test-editing lessons; the docket ranked it below the bar for independence and no new evidence has arrived since 2026-08-05 (the only new observations are the two owner reviews). Revival: one further independent sighting.
2. **hotfix-narrow-migrate-deliberately — NOT promoted.** Both citations are ONE event: 79621c60 is the mcp-2.0 tranche-1 record, 8efcea56 the transcript ADDITION relating to it. One trial in two records fails doc 10's multiple-trials law however real the lesson. The EPISODE khe:hotfix-narrow-migrate-deliberately is minted (it serves H2's citation set); the HEURISTIC is not. Revival: a second independent upstream break handled hotfix-narrow.
3. **separation-conceptual-not-nominal — NOT promoted.** Its two firewall citations are rule-statement (633b1c37) and rule-application-under-that-rule (697a50fc); the third (2a3756a1, weight-without-information) is verified by full-text read to be a DIFFERENT rule (repo weight, not conceptual separation). Revival: an independent second firewall domain showing the concept-level rule.

## Person-model design decisions
- aisv: has ZERO triples in this capsule (verified) — mirror, don't import: kh:assessedAt (xsd:date), kh:assessmentWindow (string).
- kh:fromObservation (domain kh:Episode) and rc:supportingObservation (domain rc:Pattern) both REUSE-REFUSED for claim evidence after reading their domains — kh:claimEvidence minted instead (domain kh:PersonModelClaim, range rc:Observation), SHACL minCount 2: one sighting does not model a person (the same law that gates heuristics).
- No kh:Person class at n=1 modelled persons (>=2 rule); khperson:james is a labelled IRI node; the class question goes to couldn't-say v4 with the second-pair corpus as the expected second instance.
- folding-phone stays PARKED at n=1 clear instance (the chunk-0042 GitHub-for-phone-review ask, obs ac21a9f3); that instance feeds the broader review-surface-preference claim instead. The parked term demonstrates the new shape's own >=2 gate.
- The 7 claims: burst-reviewer, random-sampling-reader, hot-cold-channels, review-surface-preference, corrects-by-adding-reasoning, explicit-competence-edge, attention-management-delegated. Evidence base: 8750c8e7 (the chunks-0027-0038 attention meta), bf8ed1c7 (tranche-3 attention meta), 5daeac50 (tranche-4 day-one meta), f327c4c5 (round-4 burst live text), 652b0215 (the eleven-day return), ac21a9f3 (phone-review ask), 2af09cfe (delegation widening), 93587901 (arc-analysis reframe), bc60c7ca / 4c28ce93 / 81a18c33 (pop-in digest + ranked queue observations).

## Execution log (2026-08-11)
- Backup: capsule copied to scratchpad pre-mutation. Start verified: 301/301 obs/evidence, conforms, zero staged debt.
- All Turtle built with rdflib (payloads-promote/gen_promote.py); every cited IRI asserted against the capsule's 301 stored observation subjects inside the generator; episode-name collision check against the 37 existing; EpisodeShape individuals asserted against the graph's typed set; serializations re-parse-checked; the one removal triple asserted verbatim-equal to the LIVE kh:AttentionProtocol comment before staging.
- **R1 (18 episodes)**: staged 4d948459 (preview conforms) → dry_run ready → applied 7ac65bb8 → conforms. map 370 → 483.
- **R2 (10 heuristics + 3 refusal graveyard entries)**: staged 5a27541f (preview conforms — the gate's first POSITIVE pass) → dry_run ready → applied 23ad6137 → conforms. map → 608.
- **NEGATIVE PROBE (gate characterization)**: a kh:Heuristic citing two REAL but untyped observations staged be20ea60 → validation_conforms FALSE, 7 violations — and the mechanism is richer than a bare sh:class hit: the validator's range inference pulls the cited observations INTO EpisodeNodeShape obligations (label/decision/fromObservation exactly-one/minCounts) they cannot meet. apply dry_run REFUSED (can_apply false, validation_failed). Row closed via stage_revision kind='review_decision' decision='discarded' (spec field is `iri`, not `revision_iri` — one sanctioned targeted error to learn it; allow_mutation_target inside spec). NO inspection-re-run slip this session: all staging outputs read from saved files, never re-invoked (the DISTILL-2 trap avoided by the taught habit).
- **R3 (person-model)**: staged 51cd0477 (preview conforms) → dry_run ready → applied 957a3095 → conforms. ontology +32/-1 (supersede-retain verified: exactly one AttentionProtocol comment remains, retained clause present), shapes +31, map +77.
- **Session meta + couldn't-say v4** recorded as observation 3e5a6554 (record_observation payload is FLAT, not spec-nested — differs from stage_revision; one targeted error to learn).
- **END STATE**: validate_graph scope=all conforms, 0 results; zero staged debt (recovery planner: 0 rows); project_brief 302 observations / 302 evidence / 13 patterns; instances live: 10 kh:Heuristic, 55 kh:Episode (37+18), 7 kh:PersonModelClaim, 12 kh:DeclinedOption (9+3).
- **Retrieval sanity**: live nodes retrieve FIRST on natural label queries (khpm:burst-reviewer, khh:graveyard-with-revival-conditions, khpm:attention-management-delegated); "seal before trial heuristic" ranks this session's own revision/patch rows above the live khh: node — the history-patch-literals-outrank-live-nodes friction, THIRD sighting (DISTILL-2 friction 5; by the corpus's counting rule this earns the structural fix a second time over).
- **export_preflight**: decision block, the SAME 4 pre-existing tranche-3 residue resources (77d54527 / a4d70dfe / ce476bd0 / 84889151); NOTHING from this session fired — including the person-model. Flag: the private-capsule-only condition is currently held by the standing block gate plus policy, not by the scanner recognizing khpm:/khperson: content; if the 4 residues were ever cleared, person-model triples would ride along unless the scanner learns the pattern. Product-ledger candidate.

## Couldn't-say v4
**Resolved this session**: v3 item 2 (attention person-terms — owner consent 813ba95e; kh:PersonModelClaim + 7 claims live) and v3 item 13's promotion half (gate exercised; ten promoted; docket discharged).
**Refused, standing**: structured sighting-counts (prose wins); corrects/confesses as relations; strict-list arc ordering; parallel arc machinery.
**Carried forward**: skill terminus (deliberately unbuilt — heuristics are the non-executable half only); context-inversion (n=1 saga); absence-by-request; outcome-status enum (case set stands); protocol-note-vs-break (2 in one trial); ElicitationRound beat-properties (await all ten rounds); contract term-kind taxonomy; limit-predicted onset (n=1); span anchoring (SourceSpan's); interruption-resume ARC (await cross-window evidence).
**New parkings**: (a) kh:Person class at n=1 modelled persons — khperson:james deliberately untyped; (b) folding-phone person-term at n=1 clear instance (ac21a9f3), feeding review-surface-preference meanwhile — the new shape's own >=2 gate demonstrated; (c) heuristic staleness signalling — heuristics carry no assessedAt-style indexicality; if second-pair evidence contradicts a promoted statement, a reconsideration mechanism at the heuristic layer is forced; (d) operator-side person-model deliberately NOT built (consent covers the owner; the operator's reflexes ARE the heuristics); (e) cross-capsule person reference (a friend's corpus gets its own capsule per a37f1c65's curator assessment; how person-models refer across capsules is unasked).
**Near-bar graveyard** (kh:DeclinedOption, revival conditions stated): promote-tests-encode-semantics; promote-hotfix-narrow-migrate-deliberately; promote-separation-conceptual-not-nominal.

## What the second-pair corpus will stress-test (pre-stated)
**In the heuristics**: (1) TRANSFER — which of the ten behavioural statements hold for a different human pair and which are James-shaped (lead-with-the-concrete is literally the owner's own rule; delegation-contract-form's altitude-setting is his style); a cross-pair citation would be a new, higher evidence tier and the first escape from the recorded one-pair-reflexivity blind spot (9f0c6fe5). (2) The three revival conditions each NAME the second corpus as their likely source — the graveyard is pre-wired to receive it. (3) The gate itself: second-pair episodes will live in a DIFFERENT capsule; whether citesEpisode can honestly span capsules is an unbuilt decision the gate will force.
**In the person-model**: (1) consent-first order is now precedent — the friend's recorded informed consent (his own 813ba95e-equivalent) must exist BEFORE any claim about him does; (2) a second aboutPerson IRI justifies minting kh:Person at >=2 and turns the claim vocabulary from one-person practice into an actual model layer; (3) contrast claims become possible for the first time (is burst-review James-specific or how reviewers behave?), which is exactly what claims-about-behaviour-with-windows was designed to survive and essence-typing would not have.

## Friction (for the product ledger)
1. History patch rows outranking live nodes in search: THIRD sighting ("seal before trial heuristic" surfaces its own staging row first). DISTILL-2 friction 5 said one more sighting earns the structural fix; it now has it. A graph-role filter or downranking of patch literals is the fix shape.
2. stage_revision kind='review_decision' takes `iri` (not `revision_iri`) and allow_mutation_target INSIDE spec; record_observation takes a FLAT payload (no spec nesting) unlike stage_revision. Two schema shapes, two sanctioned targeted errors to discover them. A consistent envelope (or error messages quoting an example payload — the review_decision error does this well, the record_observation one does not) would remove the class.
3. The negative probe's rejection mechanism is worth a doc line: citing an untyped node via a ranged property does not produce one tidy sh:class violation — range inference makes the cited node a shape target and it fails the EpisodeNodeShape battery (7 violations for 2 bad citations). Diagnosable but initially surprising; distillers should expect violation fan-out on the CITED nodes, not the citing one.
4. export_preflight does not recognize person-model content as sensitive per se (nothing new fired). Correct today because the standing block gate holds everything; if the 4 tranche-3 residues are ever cleared, khpm:/khperson: content rides along. The consent condition deserves a mechanical guard: teach the scanner the person-model namespaces, or add a standing person-model gate.
5. Positive note, worth keeping: the docket's citation hygiene held perfectly — zero dead heads across all 30+ citing IRIs (against HARVEST-4's six phantoms). The archivist's suffix-citation convention plus in-generator assertion against the capsule's own subject list is now a proven pipeline.
