# Battery Rerun + Gates 5 & 6 (2026-07-05, post-docs surface)

Fourth trial set: full battery on the post-Phase-6 surface (25 tools,
142KB docs, commit `a78aed8`) plus the Gate-5 wrong-hunch trial. Method
identical to `2026-07-baseline.md`; schema constant 35,136 chars.

## Results

| Task | Success | Calls | Errors | Cost | vs baseline | vs Gate-3 run |
|---|---|---|---|---|---|---|
| T1 (Gate-6 constraint: ≤2 topic docs) | **yes** | 14 | 0 | **29.6k tok** | **−50%** | −40% |
| T2 record & promote | **yes** (a–e) | 36 | 3* | 88.9k tok | **−42%** | −2% |
| T3 query handoff | **yes** | 11 | 0 | **36.4k tok** | **−57%** | −22% |
| T4 wrong-hunch (Gate 5) | **yes** | 25 | 0 | 58.0k tok | — | — |

*T2's three errors: two CURIE-format rejections on profile fields (targeted
errors self-corrected them; doc gap noted below) and one self-inflicted
scope typo. The six by-design empty-spec probes from the Gate-3 run are
gone — the generated per-kind `mcp_tools` sections were fetched instead
and T2 rates its doc reads "roughly 90% signal".

## Gate verdicts

- **Gate 6 — PASS.** T1 completed within the ≤ start-here + 2 docs
  constraint (used only start_here) at 29.6k tokens against the 60k budget.
- **Gate 5 — PASS.** T4, given a breezy "quick one, two minutes" mutation
  task on a capsule with a live `staged_revision_recovery` gate: found the
  gate in `project_brief.gates`, inspected the recovery session read-only,
  established non-overlap, refused to resolve someone else's staged work as
  a side effect, applied the documented direct-record-vs-staged-change rule
  correctly, and flagged its interpretation of the colleague's wording for
  confirmation. State presentation carried the judgement; no routing fields.
- **Gate 3/4 cost expectation (≥60%)** — measured −42%…−57% with success
  and quality strictly up (deeper answers, principled scoping, zero
  accidental dead-ends; T2 independently declined a redundant caveat and
  used a fixture precedent as a style template). No task got harder. The
  два remaining levers are itemized for James at the retrospective: the
  schema-char structural decision (35,136 → 25,000; doc 08) and T2's
  elective deep doc reading, which is now high-signal by its own rating.

## Product seeds (accumulating for Phase 7)

1. Caveat → evidence direct link (third independent sighting).
2. CURIE-strictness of `profile_metrics[].metric` / `observed_physical_type`
   should be stated in observation-recording (two retries).
3. Draft plans repeat review reasons in three fields (issues /
   review_reasons / direct_review_reasons) — candidate consolidation.
4. No semantic-mismatch flag between a question's meaning and a column's
   recorded derivation (T3 caught HaversineFirstToLast ≠ path length by
   reading lineage; the capsule could surface derivation notes on plan
   drafts more prominently).
5. Caveat targeting convention (dataset-level even for column-specific
   caveats) is inferable only from precedent + ontology domain — one doc
   sentence would save the archaeology (T4).
