# AIS Real-Work Study — Session 4: The Story Corpus (2026-07-06)

Fable analyst; selection built from the capsule's own recorded method
views (M1/M2/M4/M5 SQL re-run by IRI) plus three own scans; 15 stories
recorded one-at-a-time (18 MMSIs incl. pairs/flotilla), each with a
kind-line for the vocabulary distillation. Graph conforms. Only 4
broadcast-level queries needed all session — the index-first layering
held. Full detail: /home/codex/ais-study/JOURNAL-4.md and the agent
report (task a7980396593b576db).

## Story-shape vocabulary that emerged (raw material for session 5)

route-metronome; seasonal trader with berth-exact lay-ups (power-down vs
refit-dropout as inverse signatures); fixed installation (with deep-water
coverage pocket evidence); gear beacon (carried/drifting/silent trimodal);
niche-trade shuttle (live ballast-vs-cargo trap); hub-and-spokes draft
workhorse; project-fleet pair (co-location calendar as contract history);
towed work-spread flotilla; MMSI collision (simultaneous-emission dating
beats boundary dating); statics-correction reassignment (junk statics
precede the flip); corroborated-but-undatable rename; aircraft curiosity
(SOG ceiling pin); shared placeholder identity; shared default MMSI
(M1-invisible); fleet-cloned MMSI (forged call-sign corroborator).

## Method gaps recorded (distiller/M-upgrades input)

(a) M2-before-M1 precondition — boundary classes undefined on shared
MMSIs (three independent story proofs, incl. a forged corroborator);
(b) M4 type-filter misses dry-bulk laker cycles; (c) M4 needs
event→voyage pairing before cargo semantics (ballast ambiguity);
(d) MMSI-structure validation as first-pass screen (941xxxxxx gear,
111xxxxxx aircraft, placeholder families); (e) deep-water reception
pockets near platforms (coverage-envelope refinement).

## Friction

Claim recording requires spec.summary but required-ness isn't in the
targeted error (distiller item: required-split in _dispatch_kind errors
for merged-spec tools); record_pattern tolerates dangling claim IRIs
silently; analysis-view SQL discoverability (aspect="analysis_view").
Scratch-file payload passing (from the brief) eliminated quoting issues.
