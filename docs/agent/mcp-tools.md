# MCP Tools

GENERATED by tools/gen_docs.py — edit the generator, not this file.

One section per registered tool, alphabetical. Parameters marked `required`
have no default; everything else shows its default. For kind/aspect-dispatched
tools, the per-kind subsections list exactly the fields that kind accepts —
a wrong or missing field fails with a targeted error naming the valid fields,
so probe calls are never necessary. Envelope rules (absent == null == empty,
truncation fields, suggestion shape) live in the `response_conventions` doc.

## doxabase.apply_staged_revision

Apply a staged graph revision after conflict and validation checks. dry_run=True runs the read-only apply check instead (iri and validation_scope only) and returns the check response shape.

Parameters:

- `iri` (string, required)
- `applied_revision_iri` (string)
- `created_at` (string)
- `created_by` (string)
- `allow_validation_failure` (boolean, default `false`)
- `validation_scope` (string)
- `dry_run` (boolean, default `false`)

## doxabase.describe_dataset

Return bounded schema, layout, storage access, caveat, and provenance context.

Parameters:

- `iri` (string, required)
- `graph` (string | null, default `"map"`)

## doxabase.describe_query_context

Return compact non-secret query-planning context for one dataset. Pass plan_candidate ('auto', a candidate selector string, or a candidate index) to instead draft a non-executed, review-gated physical query plan for that candidate; the other planning params are only valid alongside plan_candidate.

Parameters:

- `iri` (string, required)
- `graph` (string | null, default `"map"`)
- `plan_candidate` (string | integer)
- `engine` (string, default `"duckdb"`)
- `storage_access_iri` (string)
- `physical_layout_iri` (string)
- `allow_context_blocked_candidate` (boolean, default `false`)

## doxabase.describe_resource

Describe one resource. aspect='auto' detects patterns and analysis views by rdf:type and otherwise returns the resource's triples; aspect='profile_run' needs evidence_iri (iri = profiled dataset); aspect='assertion_support' needs predicate (iri = subject; optional object/object_kind/object_datatype/object_lang).

Parameters:

- `iri` (string, required)
- `graph` (string)
- `aspect` (string, default `"auto"`)
- `include_incoming` (boolean, default `true`)
- `limit` (integer, default `100`)
- `outgoing_offset` (integer, default `0`)
- `incoming_offset` (integer, default `0`)
- `include_blank_node_closure` (boolean, default `false`)
- `blank_node_depth` (integer, default `2`)
- `blank_node_limit` (integer, default `100`)
- `evidence_iri` (string)
- `predicate` (string)
- `object` (string)
- `object_kind` (string, default `"auto"`)
- `object_datatype` (string)
- `object_lang` (string)

Aspects (`iri`, `graph`, and `aspect` always apply):

- `aspect="analysis_view"` (logical view card; graph defaults to 'map')
- `aspect="assertion_support"` (iri = assertion subject) — requires `predicate`; accepts `object`, `object_kind`, `object_datatype`, `object_lang`, `limit`
- `aspect="auto"` (detects 'pattern' and 'analysis_view' by rdf:type, switches to 'assertion_support' when predicate is supplied, else 'resource')
- `aspect="pattern"` (pattern handoff card; graph defaults to 'patterns')
- `aspect="profile_run"` (iri = profiled dataset, evidence_iri = shared run evidence) — requires `evidence_iri`; accepts `limit`
- `aspect="resource"` (paged direct triples with optional blank-node closure) — accepts `include_incoming`, `limit`, `outgoing_offset`, `incoming_offset`, `include_blank_node_closure`, `blank_node_depth`, `blank_node_limit`

## doxabase.describe_revision

Describe one revision. aspect='auto' returns revision metadata, auto-detecting staged patch rows (include_current_apply_check adds their live apply check). Other aspects: 'applied_diff' (stored before/after diff), 'version_diff' (iri = before version; requires graph_role; optional after_revision_iri/compare_to_current), 'lineage', 'resource_lineage' (requires resource_iri), 'snapshot_evidence', 'graph_snapshot' (requires graph_role). Aspect-invalid params fail with targeted errors.

Parameters:

- `iri` (string, required)
- `aspect` (string, default `"auto"`)
- `graph` (string | null, default `"history"`)
- `include_current_apply_check` (boolean)
- `include_triples` (boolean)
- `max_triples` (integer)
- `graph_role` (string)
- `after_revision_iri` (string)
- `compare_to_current` (boolean)
- `resource_iri` (string)
- `include_patch_mentions` (boolean)
- `include_apply_checks` (boolean)
- `drift_detail` (string)
- `include_applied_diff` (boolean)

Shared parameters valid for every aspect: `iri`, `aspect`, `graph`.

- `aspect="applied_diff"` — accepts `include_triples`, `max_triples`
- `aspect="auto"` — accepts `include_current_apply_check`
- `aspect="graph_snapshot"` — requires `graph_role`; accepts `include_triples`, `max_triples`
- `aspect="lineage"` — accepts `drift_detail`, `include_apply_checks`
- `aspect="resource_lineage"` — requires `resource_iri`; accepts `drift_detail`, `include_applied_diff`, `include_apply_checks`, `include_patch_mentions`, `include_triples`, `max_triples`
- `aspect="snapshot_evidence"` — no extra parameters
- `aspect="version_diff"` — requires `graph_role`; accepts `after_revision_iri`, `compare_to_current`, `include_triples`, `max_triples`

## doxabase.export_bundle

Write a reviewed export artifact. kind: 'trig' (named-graph bundle), 'graph' (flattened RDF file), 'context_slice' (importable slice TriG; spec: path, seed_iris, ...), 'staged_revisions' (review bundle; spec revision_iris is one IRI string or a list for the grouped comparison, plus path), 'profile_insight_review', 'revision_snapshots' (snapshot-row JSON), or 'handoff' (spec: trig_path, revision_snapshot_path, manifest_path, ...). Run export_preflight first; targeted errors name each kind's valid/missing spec fields.

Parameters:

- `kind` (string, required)
- `spec` (object)

### export_bundle kind="context_slice"

- `path` (str, required)
- `seed_iris` (list[str], required)
- `profile` (str, default `'dataset_brief'`)
- `max_triples` (int, default `500`)
- `include_seed_graphs` (bool, default `False`)
- `overwrite` (bool, default `False`)
- `fail_on_sensitive` (bool, default `False`)
- `fail_on_invalid` (bool, default `True`)
- `validation_scope` (str)
- `limit` (int, default `20`)

### export_bundle kind="graph"

- `path` (str, required)
- `graphs` (list[str])
- `format` (str, default `'turtle'`)
- `overwrite` (bool, default `False`)
- `fail_on_sensitive` (bool, default `False`)
- `fail_on_invalid` (bool, default `True`)
- `validation_scope` (str)

### export_bundle kind="handoff"

- `trig_path` (str, required)
- `revision_snapshot_path` (str, required)
- `manifest_path` (str)
- `graphs` (list[str])
- `revision_iris` (list[str])
- `snapshot_graph_roles` (list[str])
- `overwrite` (bool, default `False`)
- `fail_on_sensitive` (bool, default `False`)
- `fail_on_invalid` (bool, default `True`)
- `validation_scope` (str)

### export_bundle kind="profile_insight_review"

- `dataset_iri` (str, required)
- `evidence_iri` (str, required)
- `path` (str, required)
- `revision_iris` (list[str])
- `include_current_staged_work` (bool, default `True`)
- `current_staged_work_limit` (int, default `100`)
- `include_applied_staged_sources` (bool, default `True`)
- `applied_staged_source_limit` (int, default `100`)
- `title` (str)
- `executive_summary` (str)
- `format` (str, default `'markdown'`)
- `overwrite` (bool, default `False`)
- `fail_on_sensitive` (bool, default `False`)

### export_bundle kind="revision_snapshots"

- `path` (str, required)
- `revision_iris` (list[str])
- `graph_roles` (list[str])
- `overwrite` (bool, default `False`)
- `fail_on_sensitive` (bool, default `False`)

### export_bundle kind="staged_revisions"

- `revision_iris` (list[str] | str, required)
- `path` (str, required)
- `title` (str)
- `executive_summary` (str)
- `format` (str, default `'markdown'`)
- `overwrite` (bool, default `False`)
- `fail_on_sensitive` (bool, default `False`)

### export_bundle kind="trig"

- `path` (str, required)
- `graphs` (list[str])
- `overwrite` (bool, default `False`)
- `fail_on_sensitive` (bool, default `False`)
- `fail_on_invalid` (bool, default `True`)
- `validation_scope` (str)

## doxabase.export_preflight

Dry-run export privacy scope and conservative shareability decision. kind: 'handoff_bundle' (default), 'graph', 'trig', or 'revision_snapshots' preflight the matching export_bundle write; kind='scan_only' just scans graph terms for credential-like values; kind='context_slice' preflights a context-slice export (requires seed_iris; optional profile/max_triples/include_seed_graphs). Kind-invalid params fail with targeted errors.

Parameters:

- `kind` (string, default `"handoff_bundle"`)
- `graphs` (array[string])
- `revision_iris` (array[string])
- `snapshot_graph_roles` (array[string])
- `validation_scope` (string)
- `limit` (integer, default `20`)
- `seed_iris` (array[string])
- `profile` (string)
- `max_triples` (integer)
- `include_seed_graphs` (boolean)

Shared parameters valid for every kind: `kind`, `limit`.

- `kind="context_slice"` — requires `seed_iris`; accepts `include_seed_graphs`, `max_triples`, `profile`, `validation_scope`
- `kind="graph"` — accepts `graphs`, `revision_iris`, `snapshot_graph_roles`, `validation_scope`
- `kind="handoff_bundle"` — accepts `graphs`, `revision_iris`, `snapshot_graph_roles`, `validation_scope`
- `kind="revision_snapshots"` — accepts `graphs`, `revision_iris`, `snapshot_graph_roles`, `validation_scope`
- `kind="scan_only"` — accepts `graphs`
- `kind="trig"` — accepts `graphs`, `revision_iris`, `snapshot_graph_roles`, `validation_scope`

## doxabase.get_context_graph

Return a route-explained graph slice around datasets, patterns, or lore.

Parameters:

- `seed_iris` (array[string], required)
- `profile` (string, default `"dataset_brief"`)
- `max_triples` (integer, default `500`)
- `include_trig` (boolean, default `true`)
- `privacy_scan_limit` (integer, default `20`)

## doxabase.get_doc

Return one agent doc by ID (with optional section/offset slicing), or the list of available docs when doc_id is omitted.

Parameters:

- `doc_id` (string)
- `max_chars` (integer, default `12000`)
- `start_char` (integer, default `0`)
- `section` (string)

## doxabase.graph_overview

Return named graph counts, common classes, predicates, and key counts.

Parameters:

- `limit` (integer, default `25`)

## doxabase.import_bundle

Import an artifact into the capsule. kind: 'trig' (named-graph TriG file; spec: path, replace), 'revision_snapshots' (stored snapshot-row JSON; spec: path, replace), 'handoff' (recovery-complete manifest; spec: manifest_path, dry_run, replace, include_drafts, validation_scope, drift_detail), or 'example_fixtures' (bundled AIS/Polymarket fixtures; spec: replace). Targeted errors name each kind's valid and missing spec fields.

Parameters:

- `kind` (string, required)
- `spec` (object)

### import_bundle kind="example_fixtures"

- `replace` (bool, default `False`)

### import_bundle kind="handoff"

- `manifest_path` (str, required)
- `dry_run` (bool, default `False`)
- `replace` (bool, default `False`)
- `include_drafts` (bool, default `True`)
- `validation_scope` (str)
- `drift_detail` (str, default `'summary'`)

### import_bundle kind="revision_snapshots"

- `path` (str, required)
- `replace` (bool, default `False`)

### import_bundle kind="trig"

- `path` (str, required)
- `replace` (bool, default `False`)

## doxabase.list_entities

List graph entities by RDF type, graph role, and optional text filter.

Parameters:

- `type` (string)
- `graph` (string | null, default `"map"`)
- `text` (string)
- `limit` (integer, default `25`)
- `offset` (integer, default `0`)

## doxabase.list_revisions

List revision history. kind='graph' lists graph revisions (filters: revision_type, record_kind, application_status, staged_validation_status, stale_resolution_state, current_staged_work_only); kind='versions' lists graph-version snapshots for one required graph_role (exact_only, include_current); kind='resource' lists revisions anchored to or patch-mentioning a required resource_iri. include_apply_checks adds staged review routing (defaults True only for 'resource'); kind-invalid params fail with targeted errors.

Parameters:

- `kind` (string, default `"graph"`)
- `graph` (string | null, default `"history"`)
- `revision_type` (string)
- `record_kind` (string)
- `application_status` (string)
- `staged_validation_status` (string)
- `stale_resolution_state` (string)
- `current_staged_work_only` (boolean)
- `graph_role` (string)
- `exact_only` (boolean)
- `include_current` (boolean)
- `resource_iri` (string)
- `include_patch_mentions` (boolean)
- `include_apply_checks` (boolean)
- `drift_detail` (string, default `"summary"`)
- `limit` (integer, default `50`)
- `offset` (integer, default `0`)

Shared parameters valid for every kind: `graph`, `include_apply_checks`, `drift_detail`, `limit`, `offset`.

- `kind="graph"` — accepts `application_status`, `current_staged_work_only`, `record_kind`, `revision_type`, `staged_validation_status`, `stale_resolution_state`
- `kind="resource"` — requires `resource_iri`; accepts `current_staged_work_only`, `include_patch_mentions`
- `kind="versions"` — requires `graph_role`; accepts `exact_only`, `include_current`, `record_kind`

## doxabase.plan_staged_revision_recovery

Plan read-only recovery routes for staged revision queues. start_session=True persists the plan as a durable recovery session (summary, handoff_manifest_path, created_* apply only then). session_iri= without start_session describes that persisted session with a live replan; include_drafts/repair_draft_limit/drift_detail then default to the stored session settings.

Parameters:

- `revision_iris` (array[string])
- `start_session` (boolean, default `false`)
- `session_iri` (string)
- `summary` (string)
- `handoff_manifest_path` (string)
- `current_staged_work_only` (boolean)
- `include_drafts` (boolean)
- `repair_draft_limit` (integer)
- `validation_scope` (string)
- `drift_detail` (string)
- `limit` (integer)
- `offset` (integer)
- `created_at` (string)
- `created_by` (string)

## doxabase.project_brief

Return read-only capsule state: counts, dataset one-liners, gates (what is blocked and the one call that inspects each blocker), queues of pending work, and up to five suggested next actions.

Parameters:

- `limit` (integer, default `20`)

## doxabase.record_claim_reconsideration

Record that a newer claim weakens, contradicts, supersedes, or refines an older one - reconsider, never delete. The tail (reconsidered_at/by, source spans, older_claim_status) goes in spec; targeted errors name the valid fields.

Parameters:

- `newer_claim` (string, required)
- `older_claim` (string, required)
- `relation` (string, required)
- `rationale` (string, required)
- `summary` (string)
- `evidence_summary` (string)
- `evidence_sources` (array[string])
- `source_path` (string)
- `spec` (object)

## doxabase.record_graph_revision

Record revision metadata into history for a change that already happened (staged work records its own). The tail (supporting observations/claims/patterns, anchors, evidence, export path, graph counts, validation fields, identity) goes in spec; targeted errors name the valid fields.

Parameters:

- `summary` (string, required)
- `rationale` (string, required)
- `changed_graphs` (array[string], required)
- `revision_type` (string, default `"rc:ManualRevision"`)
- `spec` (object)

## doxabase.record_map_fact

Record a current-best map fact. kind: 'dataset', 'column', 'caveat', 'relationship', 'storage_access', 'physical_layout', 'partition_scheme', 'asset_transform', 'analysis_view', 'analysis_view_bundle', 'table_bundle', 'analysis_packet', 'profile_manifest', or 'profiled_parquet_table'; spec carries that kind's fields (targeted errors name valid/missing fields; see get_doc('map_authoring')).

Parameters:

- `kind` (string, required)
- `spec` (object)

### record_map_fact kind="analysis_packet"

- `iri` (str, required)
- `summary` (str, required)
- `label` (str)
- `evidence_sources` (list[str])
- `analysis_views` (list[dict[str, Any]])
- `analysis_view_iris` (list[str])
- `query_recipes` (list[dict[str, Any]])
- `artifacts` (list[dict[str, Any]])
- `followup_tasks` (list[dict[str, Any]])
- `pattern_summary` (str)
- `pattern_text` (str)
- `pattern_rationale` (str)
- `pattern_iri` (str)

### record_map_fact kind="analysis_view"

- `iri` (str, required)
- `label` (str)
- `description` (str)
- `source_datasets` (list[str])
- `row_count_snapshot` (int)
- `caveats` (list[str])
- `denominator_iri` (str)
- `denominator_label` (str)
- `denominator_description` (str)
- `denominator_row_count_snapshot` (int)
- `denominator_basis` (str)
- `query_snippet_iri` (str)
- `query_snippet_label` (str)
- `query_snippet_description` (str)
- `query_text` (str)
- `query_language` (str)
- `query_engine` (str)
- `query_snippets` (list[dict[str, Any]])

### record_map_fact kind="analysis_view_bundle"

- `views` (list[dict[str, Any]], required)

### record_map_fact kind="asset_transform"

- `iri` (str, required)
- `relationship_type` (str, required)
- `label` (str)
- `description` (str)
- `source_dataset` (str)
- `target_dataset` (str)
- `source_datasets` (list[str])
- `target_datasets` (list[str])
- `source_endpoints` (list[dict[str, Any]])
- `target_endpoints` (list[dict[str, Any]])
- `derivation_properties` (list[str])
- `conditions` (list[dict[str, Any]])
- `outputs` (list[dict[str, Any]])

### record_map_fact kind="caveat"

- `iri` (str, required)
- `description` (str, required)
- `label` (str)
- `impact` (str)
- `severity` (str)
- `targets` (list[str])

### record_map_fact kind="column"

- `iri` (str, required)
- `column_name` (str, required)
- `table_iri` (str)
- `label` (str)
- `description` (str)
- `physical_type` (str)
- `value_type` (str)
- `nullable` (bool)

### record_map_fact kind="dataset"

- `iri` (str, required)
- `label` (str)
- `description` (str)
- `is_table` (bool)
- `columns` (list[str])
- `path_templates` (list[str])
- `row_count_snapshot` (int)
- `row_semantics` (str)
- `entity_key` (str)
- `schema_stability` (str)
- `layout_verification_status` (str)
- `layout_verification_note` (str)
- `caveats` (list[str])
- `storage_accesses` (list[str])
- `physical_layouts` (list[str])
- `companion_datasets` (list[str])
- `extra_types` (list[str])

### record_map_fact kind="partition_scheme"

- `iri` (str, required)
- `label` (str)
- `description` (str)
- `partition_columns` (list[str])
- `granularity` (str)
- `path_template` (str)
- `redundant_partition_key` (str)
- `layout_verification_status` (str)
- `layout_verification_note` (str)
- `datasets` (list[str])

### record_map_fact kind="physical_layout"

- `iri` (str, required)
- `label` (str)
- `description` (str)
- `file_format` (str)
- `compression_codec` (str)
- `layout_verification_status` (str)
- `layout_verification_note` (str)
- `datasets` (list[str])

### record_map_fact kind="profile_manifest"

- `manifest` (dict[str, Any], required)

### record_map_fact kind="profiled_parquet_table"

- `iri` (str, required)
- `dataset_summary` (str, required)
- `evidence_summary` (str, required)
- `columns` (list[dict[str, Any]])
- `label` (str)
- `description` (str)
- `path_templates` (list[str])
- `row_count` (int)
- `sample_size` (int)
- `sample_scope` (str)
- `sample_method` (str)
- `observed_at` (str)
- `observed_by` (str)
- `evidence_sources` (list[str])
- `shared_evidence_iri` (str)
- `null_count` (int)
- `distinct_count` (int)
- `value_frequencies` (list[dict[str, Any]])
- `profile_metrics` (list[dict[str, Any]])
- `row_semantics` (str)
- `entity_key` (str)
- `schema_stability` (str)
- `layout_verification_status` (str)
- `layout_verification_note` (str)
- `caveats` (list[str])
- `companion_datasets` (list[str])
- `extra_types` (list[str])
- `storage_access_iri` (str)
- `storage_label` (str)
- `storage_description` (str)
- `route_roles` (list[str])
- `storage_protocol` (str)
- `access_mode` (str)
- `location_kind` (str)
- `storage_root` (str)
- `endpoint_profile` (str)
- `bucket_name` (str)
- `key_prefix` (str)
- `region` (str)
- `path_style_access` (bool)
- `credential_reference` (str)
- `storage_path_templates` (list[str])
- `storage_layout_verification_status` (str)
- `storage_layout_verification_note` (str)
- `physical_layout_iri` (str)
- `physical_layout_label` (str)
- `physical_layout_description` (str)
- `compression_codec` (str)
- `physical_layout_verification_status` (str)
- `physical_layout_verification_note` (str)
- `pattern_summary` (str)
- `pattern_text` (str)
- `pattern_rationale` (str)
- `pattern_confidence` (str | None, default `'rc:MediumConfidence'`)
- `pattern_status` (str | None, default `'rc:Tentative'`)
- `pattern_stability` (str | None, default `'rc:EmergingPattern'`)
- `pattern_map_implications` (list[str])
- `pattern_support_scope` (str, default `'all_profiles'`)

### record_map_fact kind="relationship"

- `iri` (str, required)
- `relationship_type` (str, required)
- `label` (str)
- `description` (str)
- `source_dataset` (str)
- `target_dataset` (str)
- `source_datasets` (list[str])
- `target_datasets` (list[str])
- `source_endpoints` (list[dict[str, Any]])
- `target_endpoints` (list[dict[str, Any]])
- `from_column` (str)
- `to_column` (str)
- `identifying_columns` (list[str])
- `source_columns` (list[str])
- `derived_columns` (list[str])
- `group_by_columns` (list[str])
- `aggregated_columns` (list[dict[str, Any]])
- `declared` (bool)
- `referential_integrity` (str)
- `derivation_function` (str)
- `derivation_properties` (list[str])

### record_map_fact kind="storage_access"

- `iri` (str, required)
- `label` (str)
- `description` (str)
- `route_roles` (list[str])
- `storage_protocol` (str)
- `access_mode` (str)
- `location_kind` (str)
- `storage_root` (str)
- `endpoint_profile` (str)
- `bucket_name` (str)
- `key_prefix` (str)
- `region` (str)
- `path_style_access` (bool)
- `credential_reference` (str)
- `path_templates` (list[str])
- `layout_verification_status` (str)
- `layout_verification_note` (str)
- `datasets` (list[str])

### record_map_fact kind="table_bundle"

- `iri` (str, required)
- `label` (str)
- `description` (str)
- `columns` (list[dict[str, Any]])
- `path_templates` (list[str])
- `row_count_snapshot` (int)
- `row_semantics` (str)
- `entity_key` (str)
- `schema_stability` (str)
- `layout_verification_status` (str)
- `layout_verification_note` (str)
- `caveats` (list[str])
- `companion_datasets` (list[str])
- `extra_types` (list[str])
- `storage_access_iri` (str)
- `storage_label` (str)
- `storage_description` (str)
- `route_roles` (list[str])
- `storage_protocol` (str)
- `access_mode` (str)
- `location_kind` (str)
- `storage_root` (str)
- `endpoint_profile` (str)
- `bucket_name` (str)
- `key_prefix` (str)
- `region` (str)
- `path_style_access` (bool)
- `credential_reference` (str)
- `storage_path_templates` (list[str])
- `storage_layout_verification_status` (str)
- `storage_layout_verification_note` (str)
- `physical_layout_iri` (str)
- `physical_layout_label` (str)
- `physical_layout_description` (str)
- `file_format` (str)
- `compression_codec` (str)
- `physical_layout_verification_status` (str)
- `physical_layout_verification_note` (str)

## doxabase.record_observation

Record a point-in-time finding into observations+evidence. kind='observation' (default) or 'profile' use the flat fields with the long tail (sample/profile metrics, physical/value types, ...) in spec; kind='claim' or 'query_result' take their fields in spec. Targeted errors name the valid spec fields. Observations must cite evidence (evidence_summary requires evidence_sources or a source path).

Parameters:

- `summary` (string)
- `kind` (string)
- `spec` (object)
- `observed_asset` (string)
- `observed_column` (string)
- `observed_at` (string)
- `observed_by` (string)
- `evidence_summary` (string)
- `evidence_sources` (array[string])

Kinds `observation` (default) and `profile` use the flat parameters above. Kinds `claim` and `query_result` take their fields in `spec` (top-level `summary` is merged in):

### record_observation kind="claim"

- `summary` (str, required)
- `claim_text` (str, required)
- `claim_kind` (str, required)
- `claim_targets` (list[str], required)
- `observed_asset` (str)
- `observed_column` (str)
- `observed_at` (str)
- `observed_by` (str)
- `evidence_summary` (str)
- `evidence_sources` (list[str])
- `source_path` (str)
- `source_section` (str)
- `start_line` (int)
- `end_line` (int)
- `source_kind` (str)
- `confidence` (str | None, default `'rc:MediumConfidence'`)
- `observation_status` (str | None, default `'rc:Tentative'`)
- `proposed_assertions` (list[str])

### record_observation kind="query_result"

- `summary` (str, required)
- `observed_asset` (str)
- `observed_at` (str)
- `observed_by` (str)
- `execution_status` (str, default `'succeeded'`)
- `engine` (str)
- `query_source_path` (str)
- `query_source_section` (str)
- `start_line` (int)
- `end_line` (int)
- `query_hash` (str)
- `result_sources` (list[str])
- `scanned_source_paths` (list[str])
- `scanned_source_handles` (list[str])
- `evidence_summary` (str)
- `failure_summary` (str)
- `sample_size` (int)
- `sample_scope` (str)
- `sample_method` (str)
- `row_count` (int)
- `null_count` (int)
- `distinct_count` (int)
- `value_frequencies` (list[dict[str, Any]])
- `profile_metrics` (list[dict[str, Any]])

## doxabase.record_pattern

Synthesize related observations/claims into a pattern. The long tail (synthesis provenance, source spans, status/stability, IRIs) goes in spec; targeted errors name the valid fields. An evidence_summary requires evidence_sources or source_path.

Parameters:

- `summary` (string, required)
- `pattern_text` (string, required)
- `rationale` (string, required)
- `pattern_targets` (array[string], required)
- `supporting_observations` (array[string])
- `supporting_claims` (array[string])
- `evidence_summary` (string)
- `evidence_sources` (array[string])
- `source_path` (string)
- `confidence` (string | null, default `"rc:MediumConfidence"`)
- `map_implications` (array[string])
- `spec` (object)

## doxabase.record_profile

Record profile evidence. kind: 'dataset', 'column', 'bundle', or 'domain_network'; spec carries that kind's fields (validation errors name the valid and missing fields; see get_doc('profiling')).

Parameters:

- `kind` (string, required)
- `spec` (object)

### record_profile kind="bundle"

- `dataset_iri` (str, required)
- `dataset_summary` (str, required)
- `column_profiles` (list[dict[str, Any]])
- `observed_at` (str)
- `observed_by` (str)
- `evidence_summary` (str)
- `evidence_sources` (list[str])
- `sample_size` (int)
- `sample_scope` (str)
- `sample_method` (str)
- `row_count` (int)
- `null_count` (int)
- `distinct_count` (int)
- `value_frequencies` (list[dict[str, Any]])
- `profile_metrics` (list[dict[str, Any]])
- `update_map_snapshot` (bool, default `True`)
- `allow_sampled_row_count_snapshot` (bool, default `False`)
- `map_label` (str)
- `map_description` (str)
- `is_table` (bool)
- `pattern_summary` (str)
- `pattern_text` (str)
- `pattern_rationale` (str)
- `pattern_confidence` (str | None, default `'rc:MediumConfidence'`)
- `pattern_status` (str | None, default `'rc:Tentative'`)
- `pattern_stability` (str | None, default `'rc:EmergingPattern'`)
- `pattern_map_implications` (list[str])
- `pattern_support_scope` (str, default `'dataset_profile'`)
- `shared_evidence_iri` (str)
- `column_defaults` (dict[str, Any])

### record_profile kind="column"

- `column_iri` (str, required)
- `column_name` (str, required)
- `summary` (str, required)
- `table_iri` (str)
- `observed_at` (str)
- `observed_by` (str)
- `evidence_summary` (str)
- `evidence_sources` (list[str])
- `sample_size` (int)
- `sample_scope` (str)
- `sample_method` (str)
- `row_count` (int)
- `null_count` (int)
- `distinct_count` (int)
- `value_frequencies` (list[dict[str, Any]])
- `profile_metrics` (list[dict[str, Any]])
- `update_map_column` (bool, default `True`)
- `map_label` (str)
- `map_description` (str)
- `physical_type` (str)
- `value_type` (str)
- `nullable` (bool)
- `pattern_summary` (str)
- `pattern_text` (str)
- `pattern_rationale` (str)
- `pattern_confidence` (str | None, default `'rc:MediumConfidence'`)
- `pattern_status` (str | None, default `'rc:Tentative'`)
- `pattern_stability` (str | None, default `'rc:EmergingPattern'`)
- `pattern_map_implications` (list[str])

### record_profile kind="dataset"

- `dataset_iri` (str, required)
- `summary` (str, required)
- `observed_at` (str)
- `observed_by` (str)
- `evidence_summary` (str)
- `evidence_sources` (list[str])
- `evidence_iri` (str)
- `sample_size` (int)
- `sample_scope` (str)
- `sample_method` (str)
- `row_count` (int)
- `null_count` (int)
- `distinct_count` (int)
- `value_frequencies` (list[dict[str, Any]])
- `profile_metrics` (list[dict[str, Any]])
- `update_map_snapshot` (bool, default `True`)
- `allow_sampled_row_count_snapshot` (bool, default `False`)
- `map_label` (str)
- `map_description` (str)
- `is_table` (bool)
- `pattern_summary` (str)
- `pattern_text` (str)
- `pattern_rationale` (str)
- `pattern_confidence` (str | None, default `'rc:MediumConfidence'`)
- `pattern_status` (str | None, default `'rc:Tentative'`)
- `pattern_stability` (str | None, default `'rc:EmergingPattern'`)
- `pattern_map_implications` (list[str])

### record_profile kind="domain_network"

- `dataset_iri` (str, required)
- `summary` (str, required)
- `evidence_summary` (str, required)
- `sample_size` (int, required)
- `sample_scope` (str, required)
- `sample_method` (str, required)
- `extraction_method` (str, required)
- `coverage_counts` (list[dict[str, Any]], required)
- `observed_at` (str)
- `observed_by` (str)
- `evidence_sources` (list[str])
- `evidence_iri` (str)
- `coverage_counts_exhaustive` (bool, default `False`)
- `domain_pair_counts` (list[dict[str, Any]])
- `sender_domain_counts` (list[dict[str, Any]])
- `recipient_domain_counts` (list[dict[str, Any]])
- `domain_pair_min_count` (int, default `5`)
- `allow_low_frequency_domain_pairs` (bool, default `False`)
- `analysis_view_iri` (str)
- `analysis_view_label` (str)
- `analysis_view_description` (str)
- `analysis_view_row_count_snapshot` (int)
- `analysis_view_query_text` (str)
- `analysis_view_query_language` (str)
- `analysis_view_query_engine` (str)
- `caveat_iri` (str)
- `caveat_label` (str)
- `caveat_description` (str)
- `caveat_severity` (str | None, default `'rc:Moderate'`)
- `pattern_summary` (str)
- `pattern_text` (str)
- `pattern_rationale` (str)
- `pattern_confidence` (str | None, default `'rc:MediumConfidence'`)
- `pattern_status` (str | None, default `'rc:Tentative'`)
- `pattern_stability` (str | None, default `'rc:EmergingPattern'`)

## doxabase.restage_staged_revision

Restage conflicted staged revisions against current graph state. revision_iris: one IRI string restages that revision (summary/rationale override provenance); a list runs the batch path (path/title/executive_summary export one comparison review bundle). dry_run=True writes nothing: with a string it returns the read-only rebase-draft plan; with a list, the batch would-restage preview.

Parameters:

- `revision_iris` (array[string] | string, required)
- `summary` (string)
- `rationale` (string)
- `path` (string)
- `title` (string)
- `executive_summary` (string)
- `format` (string, default `"markdown"`)
- `overwrite` (boolean, default `false`)
- `created_at` (string)
- `created_by` (string)
- `validation_scope` (string)
- `dry_run` (boolean, default `false`)

## doxabase.search

Lexical search. scope='graphs' searches literal RDF claims across named graphs; scope='staged_patches' searches staged revision patch payloads (graph defaults to history there).

Parameters:

- `query` (string, required)
- `graph` (string)
- `scope` (string, default `"graphs"`)
- `current_staged_work_only` (boolean, default `true`)
- `limit` (integer, default `20`)
- `offset` (integer, default `0`)

## doxabase.stage_revision

Stage a reviewable revision without applying it. kind: 'graph', 'map_assertion', 'systematisation', 'pattern_promotion', 'profile_map_updates', 'query_storage_access_repair', 'query_physical_layout_repair', or 'review_decision' (closes a staged row); spec carries that kind's fields (targeted errors name them). dry_run=True writes nothing, keeping the draft shapes: 'map_assertion', 'profile_map_updates' (followthrough spec fields switch to the followthrough plan), 'systematisation' (rerun), 'query_evidence_overlay' (dry-run only).

Parameters:

- `kind` (string, required)
- `spec` (object)
- `dry_run` (boolean, default `false`)

### stage_revision kind="graph"

- `summary` (str, required)
- `rationale` (str, required)
- `additions` (list[dict[str, str]])
- `removals` (list[dict[str, str]])
- `stance` (str, default `'rc:CandidateRevision'`)
- `revision_type` (str, default `'rc:StagedRevision'`)
- `included_graphs` (list[str])
- `revision_iri` (str)
- `created_at` (str)
- `created_by` (str)
- `supporting_observations` (list[str])
- `supporting_claims` (list[str])
- `supporting_patterns` (list[str])
- `revision_anchors` (list[str])
- `evidence` (list[str])
- `alternative_to` (str)
- `restages_revision` (str)
- `review_note` (str)
- `review_recommendation` (str)
- `validation_scope` (str, default `'all'`)

### stage_revision kind="map_assertion"

- `subject` (str, required)
- `predicate` (str, required)
- `object` (str | None, required)
- `rationale` (str, required)
- `change_kind` (str, default `'replace'`)
- `graph` (str, default `'map'`)
- `object_kind` (str, default `'auto'`)
- `object_datatype` (str)
- `object_lang` (str)
- `summary` (str)
- `stance` (str, default `'rc:CandidateRevision'`)
- `revision_type` (str, default `'rc:StagedRevision'`)
- `included_graphs` (list[str])
- `revision_iri` (str)
- `created_at` (str)
- `created_by` (str)
- `supporting_observations` (list[str])
- `supporting_claims` (list[str])
- `supporting_patterns` (list[str])
- `revision_anchors` (list[str])
- `evidence` (list[str])
- `alternative_to` (str)
- `restages_revision` (str)
- `review_note` (str)
- `review_recommendation` (str)
- `profile_route_sources` (list[dict[str, Any]])
- `validation_scope` (str, default `'all'`)
- `limit` (int, default `20`)

### stage_revision kind="pattern_promotion"

- `patterns` (list[str], required)
- `framings` (list[dict[str, Any]], required)
- `summary` (str)
- `intent` (str)
- `rationale` (str)
- `anchors` (list[str])
- `shared_additions` (list[dict[str, str]])
- `shared_removals` (list[dict[str, str]])
- `shared_context_summary` (str)
- `default_stance` (str, default `'rc:CandidateRevision'`)
- `revision_type` (str, default `'rc:StagedRevision'`)
- `included_graphs` (list[str])
- `created_at` (str)
- `created_by` (str)
- `supporting_observations` (list[str])
- `supporting_claims` (list[str])
- `evidence` (list[str])
- `alternative_to` (str)
- `link_alternatives` (bool, default `True`)
- `profile_route_sources` (list[dict[str, Any]])
- `validation_scope` (str, default `'all'`)

### stage_revision kind="profile_map_updates"

- `dataset_iri` (str, required)
- `evidence_iri` (str, required)
- `accepted_recommendation_indexes` (list[int], required)
- `graph` (str, default `'map'`)
- `allow_sampled_row_count_updates` (bool, default `False`)
- `allow_pending_profile_updates` (bool, default `False`)
- `summary` (str)
- `rationale` (str)
- `created_at` (str)
- `created_by` (str)
- `supporting_claims` (list[str])
- `supporting_patterns` (list[str])
- `revision_anchors` (list[str])
- `validation_scope` (str, default `'all'`)

### stage_revision kind="query_physical_layout_repair"

- `dataset_iri` (str, required)
- `layout_iri` (str, required)
- `file_format` (str, required)
- `rationale` (str, required)
- `label` (str)
- `description` (str)
- `compression_codec` (str)
- `layout_verification_status` (str)
- `layout_verification_note` (str)
- `allow_existing_physical_layouts` (bool, default `False`)
- `summary` (str)
- `review_note` (str)
- `review_recommendation` (str)
- `profile_route_sources` (list[dict[str, Any]])
- `validation_scope` (str, default `'all'`)

### stage_revision kind="query_storage_access_repair"

- `dataset_iri` (str, required)
- `storage_access_iri` (str, required)
- `storage_protocol` (str, required)
- `storage_root` (str, required)
- `rationale` (str, required)
- `label` (str)
- `description` (str)
- `route_roles` (list[str])
- `access_mode` (str)
- `location_kind` (str)
- `endpoint_profile` (str)
- `bucket_name` (str)
- `key_prefix` (str)
- `region` (str)
- `path_style_access` (bool)
- `credential_reference` (str)
- `path_templates` (list[str])
- `layout_verification_status` (str)
- `layout_verification_note` (str)
- `allow_existing_storage_accesses` (bool, default `False`)
- `summary` (str)
- `review_note` (str)
- `review_recommendation` (str)
- `profile_route_sources` (list[dict[str, Any]])
- `validation_scope` (str, default `'all'`)

### stage_revision kind="review_decision"

- `iri` (str, required)
- `decision` (str, required)
- `rationale` (str, required)
- `summary` (str)
- `resolution_revision_iri` (str)
- `created_at` (str)
- `created_by` (str)
- `review_note` (str)
- `review_recommendation` (str)
- `allow_mutation_target` (bool, default `False`)
- `validation_scope` (str)

### stage_revision kind="systematisation"

- `summary` (str, required)
- `intent` (str, required)
- `framings` (list[dict[str, Any]], required)
- `anchors` (list[str])
- `rationale` (str)
- `shared_additions` (list[dict[str, str]])
- `shared_removals` (list[dict[str, str]])
- `shared_context_summary` (str)
- `default_stance` (str, default `'rc:ExploratoryHunch'`)
- `revision_type` (str, default `'rc:StagedRevision'`)
- `included_graphs` (list[str])
- `created_at` (str)
- `created_by` (str)
- `supporting_observations` (list[str])
- `supporting_claims` (list[str])
- `supporting_patterns` (list[str])
- `evidence` (list[str])
- `alternative_to` (str)
- `link_alternatives` (bool, default `True`)
- `profile_route_sources` (list[dict[str, Any]])
- `validation_scope` (str, default `'all'`)

Dry-run planners (`dry_run=true`, nothing written; spec fields differ from the staging kind):

### stage_revision dry_run kind="map_assertion"

- `subject` (str, required)
- `predicate` (str, required)
- `object` (str | None, required)
- `rationale` (str, required)
- `change_kind` (str, default `'replace'`)
- `graph` (str, default `'map'`)
- `object_kind` (str, default `'auto'`)
- `object_datatype` (str)
- `object_lang` (str)
- `summary` (str)
- `stance` (str, default `'rc:CandidateRevision'`)
- `revision_type` (str, default `'rc:StagedRevision'`)
- `included_graphs` (list[str])
- `revision_iri` (str)
- `created_at` (str)
- `created_by` (str)
- `supporting_observations` (list[str])
- `supporting_claims` (list[str])
- `supporting_patterns` (list[str])
- `revision_anchors` (list[str])
- `evidence` (list[str])
- `alternative_to` (str)
- `restages_revision` (str)
- `review_note` (str)
- `review_recommendation` (str)
- `validation_scope` (str, default `'all'`)
- `limit` (int, default `20`)

### stage_revision dry_run kind="profile_map_updates"

- `dataset_iri` (str, required)
- `evidence_iri` (str, required)
- `graph` (str | None, default `'map'`)
- `result_bindings` (dict[str, Any])
- `staged_revision_iris` (list[str])
- `restage_stale_revisions` (bool, default `False`)

### stage_revision dry_run kind="query_evidence_overlay"

- `dataset_iri` (str, required)
- `evidence_iri` (str, required)
- `storage_protocol` (str, required)
- `storage_root` (str, required)
- `location_kind` (str, required)
- `file_format` (str, required)
- `graph` (str | None, default `'map'`)
- `storage_access_iri` (str)
- `physical_layout_iri` (str)
- `storage_label` (str)
- `physical_layout_label` (str)
- `route_roles` (list[str])
- `access_mode` (str | None, default `'rc:ReadOnlyAccess'`)
- `endpoint_profile` (str)
- `bucket_name` (str)
- `key_prefix` (str)
- `region` (str)
- `path_style_access` (bool)
- `credential_reference` (str)
- `path_templates` (list[str])
- `compression_codec` (str)
- `layout_verification_status` (str)
- `layout_verification_note` (str)
- `validation_scope` (str, default `'all'`)

### stage_revision dry_run kind="systematisation"

- `revision_iris` (list[str], required)
- `shared_context_target_revision_iris` (list[str], required)
- `summary` (str)
- `intent` (str)
- `rationale` (str)
- `link_alternatives` (bool, default `False`)
- `validation_scope` (str)

## doxabase.validate_graph

Run SHACL validation for map, ontology, patterns, shapes, or all graph scope.

Parameters:

- `scope` (string, default `"map"`)
- `limit_results` (integer, default `100`)
